---
date: 2026-09-12
context: PR #1990（盤面の飢餓を組成で見る）で、分解の穴を構造で塞ぐには 2.17.1【確定】に反する、と回した件への回答
---

## ユーザーの発言

> 確定を動かして良いです。
> PRはマージしてしまったので別PRにしてください

## エージェントの解釈

- **`board-design.md` 2.17.1【確定】「棚卸しが出す結論は `kind:` のラベル1つ」を動かしてよい。**
  棚卸しに `kind:` と `goal:` の両方を出させる形へ変える許可。こちらから出した案は
  「配れる issue は必ず `goal:` を持つ」を機械で見られるようにすること（`kind:task` なのに
  `goal:` が無い＝棚卸しの取りこぼし）で、それが通った。
- **確定は、人が動かしてよいと言えば動く。** 確定の宣言は「覆すには人間の判断が要る」であって
  「覆せない」ではない、という運用がここで実際に使われた。
- 直しは別PRにする（元のPRは既にマージ済み）。

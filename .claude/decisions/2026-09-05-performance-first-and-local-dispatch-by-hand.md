---
date: 2026-09-05
context: 盤面の道具が起こす外部プロセスを減らす案（言語を替えるのではなく境界を消す）を出し、task issue の
  立て方と、`.claude/**` を触るタスクをどこで走らせるかを訊いたときの返事
---

> (a)(b) までとし、別のtask issue にしてください。
> ただし、.claudeはクラウドセッションからはいじれないので、もし必要ならタスクセッションをローカルに起動する必要がありますが、
> まだその分岐の仕組みが入っていない気がします。
>
> パフォーマンス改善はその後の修正すべてに効くので先に実施したいです。
> タスクセッションの出し分けが今すぐ実現しないなら、あなたが直接ローカルにタスクセッションを起動する形で、このパフォーマンス改善を先に仕上げたいです。

選んだこと:

- 手は (a) 判断を純関数へ切り出す・(b) `jq`/`tr`/`grep`/`node -e` を言語の中でやる、までとする
  （`gh`・`curl` を `fetch` へ替える (c) は入れない）。
- それぞれを別の task issue にする。
- **速さの改善は他の作業より先に通す**——その後の修正すべてに効くから。
- `.claude/**` を触るタスクはクラウドから触れないので、**出し分けの仕組みが無いなら、手元から直接
  ローカルのタスクセッションを起こしてよい**。仕組みが揃うのを待たない。

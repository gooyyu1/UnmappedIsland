---
date: 2026-09-07
context: `.claude/` から取り決め・プロンプト・記録を出す先の名前（issue #1723・#1724）
---

移動先を `agents/` として issue を立てたうえで、次のように伝えた——「`agents/` という名前は、
`scripts/agent/` と1字違いで紛らわしいままです。#1724 まで済むと `agents/`（取り決めと記録）・
`scripts/daemon/`（自動実行）・`scripts/agent/`（セッションが打つ道具）で意味は割れますが、
字面の近さは残ります。今のうちに `agent-ops/` などへ変えるなら、#1723 が動き出す前に言って
ください」。

> agent-ops/ に変えてください

選んだこと: 移動先のフォルダ名は `agents/` ではなく `agent-ops/`。意味が別の場所で割れていても、
字面が近いこと自体を避ける。

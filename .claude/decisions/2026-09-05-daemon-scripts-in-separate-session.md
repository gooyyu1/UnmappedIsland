---
date: 2026-09-05
context: >-
  試験を速くする話から、本番の `scripts/agent/**` 14本の `dirname`・`tr -d '\r'` を bash の組み込みへ
  置き換える案が出た（デーモン自体も速くなる）。試験側の直しと同じPRに混ぜるか、分けるかを訊いた。
---

> デーモンの修正は別セッションにさせたいので、そのまま貼れる簡潔な指示文をください。
> シェバングの改善は入れてください。

選んだこと: 盤面を回している本番スクリプトへの変更は、試験側の直しと同じセッション・同じPRに混ぜず、
別セッションへ指示文として切り出す。

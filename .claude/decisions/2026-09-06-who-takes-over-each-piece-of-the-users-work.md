---
date: 2026-09-06
context: 司令塔の廃止でユーザーへ移った作業を列挙し（issue #1601）、自動化したいものへチェックを
  付けてもらったときの返事
---

## ユーザーの発言

> チェックをつけました。
> A1は、そもそもつける必要なし
> A2は、棚卸し係が判断
> B/Cは分析係が判断
> Dは別の時限実行エージェント、と考えています。
>
> E13は、デーモンの正常性監視と合わせて「監視」というセッションですでに実行しています。
>
> ただし、このセッションは私が手動で立てたものなので、「監視係」エージェントとして正式に定義し、
> 該当セッションがなければ自動で開始するような仕組みとしたいです。

## エージェントの解釈

- **`判断待ち` は棚卸しが付けない。** 「そもそもつける必要なし」。
- **順序（`blockedBy`）を張るかは棚卸し係が判断する。** ユーザーの手番にしない。
- **PRから人へこぼれるもの（`## ユーザーへ`・`## 見た目`・`## 仮決め`・担当外の直し・立てられた
  issue を列に載せるか）は、分析係が判断する。**
- **`.claude/decisions/` の棚卸しは、別の時限実行エージェントが実施する。**
- **デーモンの正常性監視は「監視係」として正式に定義し、該当セッションが無ければ自動で始める。**
- **チェックの付かなかった項目は、人の手番のまま。** どれかは issue #1601 のチェックが持つ。

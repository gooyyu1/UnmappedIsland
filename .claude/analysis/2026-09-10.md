# 2026-09-10 の分析

## 読んだ範囲

マージ済みのPR30本（#1770〜#1838。`scripts/agent/board-read.mjs` の `MERGED_LIMIT`）のコメントを
当たった。👀 の付いていなかったのは #1838 の1件だけで、そこに `[スメル] ` の行が2つ。

作成順の窓から外れる遅れマージが無いことも確かめた——`merged:>=2026-09-09` で引くと #1838 の1本
だけで、前の周（#1838）が拾った「人の判断を待って遅れて入ったPR」に当たるものは、この帯には
残っていない。

## 切った issue

- **#1839** — 拾ったスメルを issue にしなかったときに何を書くかを、一次の係の本文
  （`.claude/analysis-prompt.md`）が定めていない。前の周の記録が「この周から理由を残す」と
  決めているが、一次の係は過去の回の
  ファイルを読まないので、**記録の側には次の周へ届く経路が無い**。この周へ届いたのは、外の
  レビュアーがその2行をスメルとして引いていたからで、引かれなければ読まないまま走った（#1838）。
- **#1840** — 前の回の記録を名指すスメルを、一次の係が確かめられない。この帯の未読スメル2件は
  どちらも `.claude/analysis/2026-09-09.md` の行を指しており、**現物と合っているかを見る手が、
  読むなと言われているファイルを開くことしか無い**。分析係のPRは毎回出るので、帯が回るたびに現れる。
- **#1841** — `#656` の項目番号で出どころを指す記述が `docs/world/ContentSkeleton.md:171` に1箇所
  残っている（#1838）。`同 13` の綴りなので、`の <番号>` で引いた #1834 の一覧の外。あちらの
  `## 完了の条件` は「上の13箇所」と数え上げているので、済ませてもここは残る。
- **#1845** — ひな形の囲みが本文を丸ごと覆っている `*-prompt.md` は、節を名前で引けない。
  `docReferences.test.ts` は見出しをフェンスの外からしか拾うので、`analysis-prompt.md` は題名以外の
  見出しが全部囲みの中にある。**この記録が自分の係の本文の節を引いてCIが赤くなった**ので、節名を
  落とす形で回避した。

**落としたスメルは無い。** 読んだ2件はどちらも issue にした。#1840 と #1845 は読んだスメルからでは
なく、この周を走らせて踏んだもの。

## 傾向

**なし。** 今回の帯で未読だったのは1本のPRの1コメントだけで、繰り返しを言える材料が無い
（一次の係の本文が、1本のPRにしか出ていない形は傾向ではないと定めている）。2件とも手を打つ側へ
回した。

帯が薄かったのは取りこぼしではない。前の周（#1838、2026-09-09 12:36 マージ）から今日までに
`main` へ入ったPRが #1838 自身の1本だけで、他の29本には前の周までの 👀 が付いている。

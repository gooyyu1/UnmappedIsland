# 設計ドキュメントの書き方

`docs/` 配下のドキュメントが従う定型です（**確定度の印の条件だけは、印を使う文書すべてに掛かります**
——6 節）。文章そのもののスタイル（簡潔さ・コメントにも同じく掛かる
「経緯を書かない」等）は [`CLAUDE.md`](../CLAUDE.md) が持ち（**その禁止が掛からない文書の別は
9.1 節**）、本書は**構造**——見出し・節番号・概要・実装状況の表し方——と、
**文書へ何を書き残し、何を書かないか**を決めます。リンクと参照の解決は
自動テスト（`tests/docs/docReferences.test.ts`）が検査します。

## 1. 文書の種別

定型は種別ごとに違います。どの文書がどの種別かは各フォルダの `README.md` の分類に従います。

| 種別 | 例 | 構成 |
|---|---|---|
| 設計 | `HuntingSystem.md`、`docs/ui/` の各文書 | 概要（2 節）＋論点ごとの節＋未決事項 |
| リファレンス | `GameElementDefinition.md`、`Localization.md` | 位置づけ＋項目の網羅＋対象外。概要は要らない |
| カタログ | `Animals.md`、`Containers.md` | 分類の軸＋一覧＋住み分け＋未決事項 |
| 実装ガイド | `TerrainGenerationImplementation.md` | 呼び出し関係＋索引。設計理由は書かず設計文書へリンク |
| 教訓集 | `DesignNotes.md`、`DesignPrinciples.md` | 結論を見出しにした項目の列挙 |
| 測定 | `docs/diagnostics/` | [`diagnostics/README.md`](./diagnostics/README.md) に従う |

実装ガイドの索引と `README.md` の収録一覧は、[`CLAUDE.md`](../CLAUDE.md)「数え上げを書かない」が
禁じる書き写しには当たりません。**行ごとに、在り処を見ても出ない対応**——どの節が説明しているか・
その文書が何を決めるか——を持つためです。**その対応を持たせられないなら、一覧は置かずに在り処を
指します。** 個数は、対応があっても書きません。

**ただし、各フォルダの `README.md` だけは一覧を省けません**（分類がそこにしか無いので、対応は必ず
書けます）。置き場が合っているかの跡は、そのフォルダの一覧がその文書を指しているかどうかにしか
残らないためです——実装全般の規約が `docs/engine/` に在るのに engine の一覧へ載っていない、という
形で現れます（`tests/docs/docFiling.test.ts` が検査します）。

## 2. 設計文書の概要は、次の問いに答える

この順で、最初の画面に収まる分量で書きます。模範は
[`LiquidContainerSystem.md`](./engine/LiquidContainerSystem.md)（実現方法が明快）と
[`ExplorationSystem.md`](./engine/ExplorationSystem.md)（境界の書き方が明快）です。

1. **守備範囲** — この文書は何を決めるか。
2. **境界** — 何を決めないか。隣の文書はどれか（リンク）。
3. **実現方法** — この文書が扱う概念を、エンジンのどの機能へどう写したか。どの trait・タグ・
   スロット名・プロパティ名を使うか（＝この文書が導入する語彙）。新設した文法は何か。
   **新設ゼロならそれを明記します**——汎用機能に載せた場合こそ、写し方の記述が唯一の設計判断です。
4. **在処** — 定義（`src/assets/world-codex/*.yaml`）・実装（`src/`）・検証（`tests/`）はどこか。
5. **確定度と実装状況** — 検討結果か確定仕様か。未決事項は何節か。未実装の節はどれか（4 節）。

節の一覧をなぞる「目次の要約」は書きません。目次で分かることに一等地を使ううえ、全節を挙げるため
網羅的に見え、書かれていないこと（実現方法の欠落など）に気付けなくなります。

## 3. 見出しと節番号

- **節番号が識別子です。** 他の文書やコードからの参照は節番号で行うため、**既存の節番号は
  付け替えません**。節の追加は末尾か枝番（`4.1` の後に `4.2`）で行います。
- 見出しには**その節が決める論点**を書き、**結論は本文の先頭 1 文に太字で**置きます。結論を
  見出しに書くと、結論の推敲のたびに参照が壊れます（名前で参照している場合）。ただし結論を
  見出しにした方が目次が読みやすい場合は禁じません——その見出しを名前で参照しないだけです。
- `概要` と `未決事項・今後の検討課題` は番号付き節の外に置いてもかまいません（既存文書の慣例）。

## 4. 実装状況は宣言し、丸ごと未実装の節にラベルを付ける

ドキュメント先行で書かれた設計は、実装済みの節と未実装の節が同じ断定調で並び、読み手の判断を
誤らせます。

- **文書全体の実装状況は概要で 1 回言います**（模範: [`ClimateSystem.md`](./engine/ClimateSystem.md)）。
  多数派を既定として宣言し、例外の節だけに印を付けます。
- **節の主題が丸ごと存在しないときだけ**、見出しの末尾に `【未実装: 識別子】` を付けます。
  識別子はその節が導入する語彙から 1 つ選びます（例: `【未実装: 海図】`）。子節はラベルを
  継承するので繰り返しません。
- 識別子を書くのは検証のためです。自動テストが `src/` を照合し、実装されたのに剥がし忘れた
  ラベルを検出します。識別子の無い `【未実装】` は検証できないので使いません。
- **節の一部だけが未実装の場合はラベルにせず、本文で書きます。**「一部未実装」というラベルは
  結局本文を読まないと判断できず、警告として機能しません。
- 語彙は「未実装」の一語に統一します（「未対応」「実装していません」等を混在させない）。
  ラベルは TODO 管理ではありません——目的は読み手の判断を誤らせないことだけで、進捗表として
  細かく付けるほど更新漏れが増えます。

### 4.1 いつか実装したいものは、別の印と一覧で分ける

`【未実装: 識別子】` を付けるのは**無いと完成と言えないもの**だけです。**無くても完成を宣言できるが、
いつか実装したいもの**は `【いつか: 識別子】` で分けます。1つの印で両方を扱うと、**残りの作業量を
誰も答えられなくなります。**

| 印 | 意味 | 実体はどこ |
|---|---|---|
| `【未実装: 識別子】` | 無いと完成と言えない。残作業 | `kind:task` の issue |
| `【いつか: 識別子】` | 無くても完成を宣言できる | [`Someday.md`](./Someday.md) に必ず1項目 |

- **印と項目は1対1です。** 項目の無い印も、印の無い項目も、自動テストが落とします。
- **節の主題が丸ごと「いつか」なら見出しの末尾へ、節の一部ならその節の本文に1行**書きます
  （`【未実装】` と同じ切り分け）。本文に書く形は「印 ＋ 何を待っているかの1文 ＋ `Someday.md` への
  リンク」で、**中身は書きません**——詳しい説明は `Someday.md` にだけ置きます。
- **「未決事項・今後の検討課題」節は、決まっていないこと専用です。** やると決まっていて、やっていない
  だけのものは、どちらかの印へ振り分けます。
- 確定度（6 節）とは別の軸です。両方付くときは `【確定】` を先に書きます。

## 5. 参照の書き方

- 他の文書の節は **`文書名.md N節`** で指します。コメント・YAML では要旨を添えます
  （例: `GameElementDefinition.md 6.4節のalert`）。番号だけでは読み手に何の話か伝わらず、
  名前だけでは見出しの推敲で切れるためです。
- 番号だけの参照（「4 節」）は**同じ文書の節**を指します。コード・YAML では
  `GameElementDefinition.md`（WorldCodex 文法）の節を指す既定とします。「同 N節」は、同じファイル内で
  **直前に名前を挙げた文書**の節を指します。文書名の言及から離れた場所で、他の文書の節を番号だけで
  指してはいけません——読み手にも自動テストにも解決できません。
- 節番号を持たない見出し（`Localization.md` の項目など）は
  **`文書名.md 〇〇節`** と名前で指せます。自動テストが実在を検査します。
- 文書内・文書間の Markdown リンク（`[<表示名>](#<アンカー>)` / `[<表示名>](<パス>#<アンカー>)`）も
  自動テストが解決を検査します。
- **検査はコードフェンスの中も本文と同じに見ます。** YAML の例に添えたコメントの参照も、指し先が
  動けば嘘になるためです（確定度の印（6 節）はこの逆で、フェンスの中を見ません——そちらは規約が
  書式そのものを見せる場所だからです）。
- **今は無い名前は `Xxx.yyy` の形で書きません。** その形は「今も在るものを指す参照」として自動
  テストが検査するためです。消えたメンバーを語るなら「`Xxx` に生えていた `yyy`」のように所有者と
  メンバーを切り離します（そもそも書けるのは、経緯を主題とする文書だけです。9.1 節）。
- **参照ではなく書式そのものを見せたい行は、指し先として読めない形で書きます**——リンクなら
  `<パス>` のようなプレースホルダ、節の参照ならファイル名を日本語にした `文書名.md N節`。
  **囲み（バッククォート）は逃げ道になりません**——囲みは「これは識別子だ」を表す記法で、参照の
  書き手は指し先を明示するために付けているためです。

## 6. 確定していることにだけ印を付ける

決定は後から覆ります。**印の無い記述を暫定**——今はこう書いてあるが覆りうる——という既定にし、
覆すのに判断が要るものにだけ印を付けます。

- **確定した節は、見出しの末尾に `【確定】` を付けます。** 意味は「**覆すには人間の判断が要る**」。
  読み手は従い、疑問があれば書き換えずに問いとして出します。
- **印が無い節は暫定です**（文書単位の宣言がある文書を除く。6.2 節）。従ってよいが、矛盾や不都合を
  見つけたら**覆す提案を出してよい**。決まっていない箇所に仮決めを書いたときも、印を付けずにここへ
  入れます。
- **印は確定の側にだけ付けます。** 大半は暫定なので、暫定に印を付けると全ページに印が並び、
  背景になって読めなくなります。**印は少数側に付けたときだけ印として働きます。** 暫定の側に印を
  付ける語（`【未確定】`・`【暫定】`）は使いません。自動テストが検出します。
- **`【確定】` は内容が正しいという主張ではなく、変更権限の宣言です。** 確定節の記述が実装や他の節と
  食い違っていても、直さずに問いとして挙げます。「動いている方が正しい」で上書きすると、
  印の意味が消えます。
- **確定度と実装状況（4 節）は別の軸です。** 確定した未実装の節も、暫定の実装済みの節もあります。
  両方付くときは `【確定】` を先に書きます（`### 節番号 見出し【確定】【未実装: 識別子】`）。
- 概要の「確定度と実装状況」（2 節）は文書全体の要約として残しますが、**節の印が優先します。**
- **印の条件は、`docs/` の外で印を使う文書にも同じに掛かります**（`CLAUDE.md`・`.claude/**`）。印の
  意味は置き場で変わらないので、条件も変わりません。**日付ごとの記録**（`.claude/decisions/**`・
  `.claude/analysis/**`）**は外します**——当時の発言と観測を原文のまま残す場所で、印はそこでは
  題材として現れます。**条件を広げた時点で満たしていなかった印は、検査の側が節を名指しで据え置いて
  います**（`tests/docs/docReferences.test.ts`）——外すのはユーザーの判断（6.1 節の最後）なので、
  検査を理由に外せません。**新しく付ける印は、据え置きではなく条件を満たす側で通します。**

文書ごとの節数・確定節数・未実装節数は `npm run stats:docs` が一覧にします。

### 6.1 印を付けてよいのは、射程・中身・出どころ・権限を満たす節だけ

**`【確定】` は「決定事項に、人間の判断していないことが混ざっていない」ことを保証する印です。**
混ざり始めた時点で、決定と暫定の区別が付かないという**印を作る前の状態へ戻ります**。混ざる経路を
塞ぐのが、見出しに挙げた条件です。射程・中身・出どころは `tests/docs/docReferences.test.ts` が検査します。権限は
機械では判定できないので、**出どころの1行が書けるか**という形で出どころの検査に載せています。

- **射程はその節の本文だけで、下位節には及びません。** そのため**確定節は下位節を持ちません**——
  抱えているなら親には付けず、確定した子だけに付けます（[`GameEndings.md`](./concept/GameEndings.md)
  8.1節 がこの形です）。親に付けると、見出し1つで下位節の全行が確定になり、どこまでが人間の判断
  だったかを読み手が数えられません。**`【未実装】`（4 節）が子節へ継承するのとは逆です**——あちらは
  主題が無いという事実なので子にも当てはまりますが、こちらは節ごとの変更権限の宣言です。
- **書けるのは、人間が判断したことだけです。** そこから導いた数値・記法・実現方法・名前は仮決め
  なので、印の外（枝番の節・未決事項）へ出します。**本文に「目安です」と添えても効きません**——
  印は見出しに付くので、但し書きは印の内側にあります。導出まで囲うと、**導出の誤りが人間を通さ
  ないと直せなくなります**。暫定を表す語（「目安」「仮置き」など）が確定節の本文に出ないことを
  自動テストが検査します。
- **本文に、出どころの1行を持ちます。** 形は `**出どころ**:` で始まる1行で、指すのはユーザーが
  判断した場所です。

  ```
  ### 8.1 1 周回の長さは、作る物の量から出た実測に合わせる【確定】

  **1 周回の長さは、作る物を積み上げて出た日数をそのまま想定にします。**

  **出どころ**: [#656](https://github.com/gooyyu1/UnmappedIsland/issues/656)（1 周回の長さは実測に合わせる）
  ```

  **指せる番号があるなら番号で、無ければ何を決めたかで指します。**
  [issue #656](https://github.com/gooyyu1/UnmappedIsland/issues/656) の項目番号は「指せる番号」では
  ありません——答えの済んだものから詰め替わるので、**書いた当時に正しくても、後から別の決定を指します。**
  指すのは issue そのもの（`#656`）と、**何を決めたか**です。

  **#656 を通っていない決定——対話の中で決まったもの——は、`ユーザーの指示` と書きます**
  （[`GameConcept.md`](./concept/GameConcept.md) の宣言と [`Voyage.md`](./world/Voyage.md) 2.3 節が
  この形です）。**指す先が無いからと `#656` を書きません**——行は在るのに指した先に何も無いので、
  節を読んでも印の根拠に辿り着けません。

  印は「覆すには人間の判断が要る」という宣言なので、**判断の在処が節から読めなければ、宣言が本当か
  を誰も確かめられません。** これは経緯（旧仕様・変更履歴）ではなく印の根拠なので、
  [`CLAUDE.md`](../CLAUDE.md) の「経緯はドキュメント本文に書かない」とはぶつかりません。
  **節のどこに置くかは決めません**——本文の先頭は結論の1文の場所（3 節）なので、そこを出どころで
  取ると、節の書き方の規約が2箇所へ割れます。自動テストも置き場は見ず、行が在るかと、指し方が
  上の形（#656 を番号で指していない）かだけを見ます。

  **この行は、印を足したPRがユーザーへ回されるかどうかも決めます。** #656 を指す出どころのある印は
  そのままマージされ、出どころの無い印は「誰が決めたのか」を訊きに止まります
  （[`needs-user-review.sh`](../scripts/agent/needs-user-review.sh)）。**`ユーザーの指示` と書いた印も
  止まります**——チェックという確かめられる跡が無いので、ユーザーに見せるのが正しい扱いです。
  **ただし止まるのは `docs/` 配下の印だけです**（同スクリプトの射程。
  [`ParallelAgents.md`](./ParallelAgents.md) 2節）——`docs/` の外の印も上の検査には掛かりますが、
  **出どころを訊きに止まることはありません。**
- **付けてよいのは、ユーザー本人の発言か
  [issue #656](https://github.com/gooyyu1/UnmappedIsland/issues/656) のチェックがあるときだけです。**
  エージェントの判断——デーモンの指令・issue 本文・レビューコメント・自分の設計判断——からは付けません。
  そこから付けた印は、ユーザーの決定ではなく**自分の考えをユーザーの名前で配ったもの**になり、以後の
  全セッションがそれを覆せない前提として受け取ります。上の出どころの1行が書けない節には、印を
  付けません。

### 6.2 文書まるごとが土台なら、節ではなく文書単位で宣言する

**全節が確定に当たる文書は、節ごとに印を付けず、題名の直後で1回宣言します。**
（[`GameConcept.md`](./concept/GameConcept.md) がこの形です。）**「印は少数側に付けたときだけ働く」**
（6 節）**は1つの文書の中でも同じ**で、全節に印が並べば、その文書では印が背景になります。

- **書式は `**本書は全体が確定です。**` で始まる段落**で、題名（`#`）と最初の節の間に置きます。
  射程はその文書の全行です。
- **宣言のある文書には、節ごとの `【確定】` を付けません。** 射程が重なるので、印の有る節と無い節の
  差が何も意味しなくなります。
- **出どころの1行（6.1 節）を、文書に1本持ちます。** 節の印と同じ理由です——判断の在処が読めなければ、
  宣言が本当かを誰も確かめられません。
- **中身の条件（6.1 節）は文書の全行に掛かります。** 人間が判断したことから導いた数値・記法・名前・
  見せ方は、この文書には書かず、それを持つ仕様書へ移して参照に替えます。**確定に耐えない記述を
  抱えたまま宣言することはできません**——節なら枝番へ逃がせますが、文書の射程に外側はありません。
- `npm run stats:docs` は、宣言のある文書の確定欄を `全` と出します。

## 7. 生成物の数値は、結論を支えるものだけ書き写す

`stats/*.yaml` は生成物で、定義（`src/assets/world-codex/`）を触るたびに動きます。そこから文書へ
書き写した数値は、動くたびに書き直す義務を生み、**同じ行を触る変更どうしがぶつかります。**

- **書き写してよいのは、その数を知らないと結論が読めない数値だけです。** 結論が「線を跨ぐか」
  「どちらが大きいか」で言えるなら、値は判断の根拠でしかないので書かず、`stats/` のどのファイルの
  どの節かだけを指します。読み手が確かめたくなったらそこを開けば、今の値が読めます。
- **同じ値は1箇所にだけ書きます。** 他の節からは、その節を番号で指します。2箇所に書くと、片方だけが
  古くなるか、生成物が動くたびに両方を書き直すことになります。
- **生成物の表は、そのまま写しません。** 表は `stats/` に1本だけ持ち、文書にはそこから読み取れる結論
  だけを書きます（[`ContentSkeleton.md`](./world/ContentSkeleton.md) 8.1.3節・8.3節がこの形）。複数の
  節から組み立てた要約は表にしてかまいません（同 8.3節の局面ごとの表）——写しではないので、生成物の
  側に置き場がありません。
- **書き写した数値には、出どころのキーを名乗る印を直後に置きます。** 印の無い数値は照合に載らず、
  再生成で黙って古くなります。書式と、下位桁が主張に効いていないときに書く粗さは
  [`diagnostics/README.md`](./diagnostics/README.md) が持ちます。

この線は生成物にだけ掛かります。人が書いた定義から引いた数値は、再生成でずれる問題を持ちません。

## 8. 数の推移を見せるとき

**運用の決め方で動く数を推移として出すときは、数だけを出しません。** 7 節の生成物の数値と違い、
こちらは測り方も方式も変わり続けるので、値そのものだけでは何の記録なのかが読めません。

- **何の方式の記録なのかと、1件あたりの規模を添えます。** これらは成果ではなくやり方の記録で、
  分母が付いて初めて変化の向きが決まります。
- **「今日時点」として書いた規模の数値は、後の作業でずれても測り直さず、測った日のまま据え置きます。**
  この種の数値はどうせ動き続けるので、追いかけても正確になる先がありません。効くのはいつ測ったかです。
- **全体の一部しか覆っていない指標は、測れている範囲を注記して載せるのではなく、消します。**
  表に数がある限り、注記より先に数のほうが全体の話として読まれます。

## 9. 文書は、失敗の履歴で作らない

**書くのは、今どうなっているかと、それが何のために要るかです。** 踏んだ失敗から書き起こすと、
文書は起きたことの一覧になり、起きなかったことを構造的に落とします。

- **誤読された記述は、「〜ではない」と打ち消しを足すのではなく、元の記述の曖昧さを直します。**
  打ち消しは起きた1つの誤読しか塞ぎません。曖昧さが残る限り、次は別の形で誤読されます。
- **仕組みは、踏んだ失敗を並べるのではなく、何のために要ったかとして説明します。** 失敗の一覧は、
  先回りできて問題にならなかったものを構造的に落とします。
- **その仕組み以前から在ったものを、その仕組みの前提として書きません。** それが無くても要るものと、
  その仕組みで初めて要るものを分けます。混ぜると、その仕組みが実際に足したものが1つも残りません。

### 9.1 経緯を書いてよいのは、経緯そのものを主題とする文書だけ

**過去の姿を書き残す場所は、それを主題とする文書に限ります。** ここに挙げていない文書では、
[`CLAUDE.md` の「ドキュメント・コメントのスタイル」](../CLAUDE.md#ドキュメントコメントのスタイル)
の禁止どおり、経緯（旧仕様・変更履歴・不採用案・検討過程）を本文へ書きません。

| 文書 | 主題 |
|---|---|
| [`engine/DesignNotes.md`](./engine/DesignNotes.md) | 繰り返さないために残す価値のある、過去の失敗・不採用の決定 |
| [`HowWeGotHere.md`](./HowWeGotHere.md) | 作り方そのものをどう変えてきたか |

**次の項目は、この表の文書の中での書き方です**——他の文書には掛かりません。そちらには、書き方を
決める対象そのものがありません。

- **変えてきた経緯は、全部を貫く1本の理由で説明せず、回ごとの理由をそのまま並べます。** 1本の筋へ
  揃えると、筋に乗らない回の理由が事実のほうから削られます。
- **ある段が何だったのかは、後で何に効いたかではなく、当時何のために要ったかで書きます。** 効いた先
  から逆算した説明は、そこへ辿り着くことを最初から狙っていたことにしてしまいます。

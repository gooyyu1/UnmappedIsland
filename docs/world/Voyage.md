# 航海（筏・外洋・本土）

## 概要

島から出航して本土へ渡るまで——**筏を作り、積み、漕ぎ出し、風と海流に運ばれ、着く**——を、ゲームに
登場するモノとしてどう表すかをまとめます。周回の終わり方そのもの（なぜ出航だけをエンドにするか、
何を持ち帰れるか）は [`GameEndings.md`](../concept/GameEndings.md) が決めており、本書はそれを
エンジンのどの機能へ写したかを持ちます。

決めないことは、航海の外側です。海図と推定日数の提示（同 9 節）、中盤の沿岸航海（同 11 節）、
待機アクション（同 10 節）はまだありません。飢え・渇きは航海中も島と同じに働きます
（[`VitalsSystem.md`](../engine/VitalsSystem.md)）。季節と天気は
[`ClimateSystem.md`](../engine/ClimateSystem.md) が持ち、風向きだけを本書が足しています（3.1 節）。

**新しい文法は 1 つだけです。** 筏も外洋も本土も普通の `location`（[`ExplorationSystem.md`](../engine/ExplorationSystem.md) 1 節）で、
進みは `stages` + `passives` の `add`、速さは `modify` の合算、到達は `move` です。足したのは
`move` の行き先を型で指す `to_object`（[`GameElementDefinition.md`](../engine/GameElementDefinition.md) 9.6 節）
だけで、これは世界にただ1つ在る場所（`singleton`、同 15 節）を、生成時のインスタンスIDを知らずに
指すためのものです。

在処は、定義が `src/assets/world-codex/voyage.yaml`（筏・外洋・本土）・`timber.yaml`（木と丸太）・
`artifacts.yaml`（持ち帰る物）・`core.yaml`（風向き）、周回をまたぐ棚が `src/save/Shelf.ts` と
`src/game/ShelfScene.ts`、到達したことの読み取りが
`src/domain/runtime/views/PlayerCharacter.ts`、検証が `tests/world-codex/voyageYaml.test.ts` と
`tests/world-codex/timberYaml.test.ts`・`tests/save/shelf.test.ts` です。すぐ試せる開始状態として
`src/assets/scenarios/voyage_ready.yaml` があります。

本ドキュメントは検討結果であり、確定仕様書ではありません。**全体が実装済みです。** 未決事項は
[未決事項・今後の検討課題](#未決事項今後の検討課題)にまとめています。

## 1. 丸太は斧だけが作り、1本ずつしか運べない

**筏の材料は丸太とロープで、丸太は斧で木を伐り倒すことでしか手に入りません。**

刃物（`cutting_tool`）で足りる草の茎・ヤシの葉と違い、木部を持つ幹は柄の遠心力が乗る道具でなければ
断てません。斧を指すのは `chopping_tool` タグで、今これを持つのは石斧だけです。石を打ち割って刃を作り、
柄を付けて斧にする——それまでの道具作りが、そのまま出航の前提になります。

丸太は1本20kgで、担ぐと荷重（`load`）の段が1つ上がります。**2本目は担げません。** 筏1つに6本要るので、
どこで伐ってどこへ運ぶかが島側の仕事として残ります（[`GameEndings.md`](../concept/GameEndings.md) 3.1 節）。

## 2. 筏は設置物であり、場所でもある

**筏（`raft`）は `fixture` タグを持ちながら `location` trait を実装します。** 土地と同じ
`items`/`fixtures`/`characters` の3スロットを持つので、積荷も乗り手も筏の**中**に入ります。

この形にすると、出航は「筏を1つ動かす」だけになります。積荷もプレイヤーも子として付いてくるので、
「何を一緒に運ぶか」を数える処理はどこにも要りません。

- 積み込みは編み籠と同じ操作です（`main_item_slot: items`）。カードを押せば積荷が並び、重ねれば入ります。
- 積める量は枠の数（12）とかさ（500L）が決めますが、**実際の限界は重さ**です（3.2 節）。
- 乗り込むのは出航アクションの一部です。**降りられるのは本土だけ**なので、乗る手と出る手を分けても
  選べることは増えません。

## 3. 航海の進みは、速さの段が毎tick刻む

**筏は `voyage_progress`（本土までの道のり）を持ち、`sail_speed` の段が毎tickそこへ加算します。**
`range.max` が航海の距離で、追い風が続けば8日ほど、風に恵まれなければその倍以上かかります。

速さを直接足せないのは、`add` の量がリテラルしか書けないためです
（[`GameElementDefinition.md`](../engine/GameElementDefinition.md) 9.2 節）。**速さを段に刻み、段ごとに
違う量を足す**ことで、可変の速度を専用の仕組みなしに表しています。

| `sail_speed` の段 | 1tickの進み |
|---|---|
| `becalmed`（0〜1） | 0 |
| `slow`（2〜3） | 1 |
| `moderate`（4〜6） | 2 |
| `steady`（7〜9） | 3 |
| `fast`（10〜） | 4 |

`sail_speed` の素の値は0で、寄与は4つ（風・海流・積載・帆）だけです。**陸に居る間はどれも効かない**ので、
浜に繋いだ筏の進みは常に0になります。

帆の寄与だけは、条件の書き方が他と違います。筏自身の寄与は親の型で絞れますが（`parent` が
`sea` タグを持つか）、**組み込んだ部品から見ると海は親の親で、条件では指せません**。そこで外洋が
`afloat` を子へ配り、帆はそれを読みます。条件を落とすと、浜に置いた筏の `sail_speed` が2になって
段が `slow` へ上がり、陸に居るまま本土へ着いてしまいます。

### 2.1 帆は構造スロットへ組み込む

**帆は積荷ではなく筏の一部です。** 筏は `structure` スロット（[`Dwellings.md`](./Dwellings.md) 1 節の
構造スロットと同じ形）を1枠だけ持ち、`sail` タグを持つ物を1つ受け入れます。組み込んだ帆は
`modify` で親（筏）の `sail_speed` を +2 します。

今ある帆は生皮の帆（`rawhide_sail`）1種で、生皮6・糸10・太い枝2・ロープ2から、骨針（`sewing_tool`）を
道具として縫います。**+2 は、風のどの状態でも段をちょうど1つ上げる最小の値です**——向かい風では
素の筏が `becalmed` で止まるところ、帆があれば `slow` に届いて進み始めます。+3 にしても向かい風の
段は変わらないので、盤面に差が出ません。

**構造スロットは、筏に乗り込んでから現在地の札を押すと開きます**（積荷のスロットは外から札を押して
開くので、2つは操作の入口から分かれています）。乗り降りは筏の `board`/`disembark` です。

### 3.1 風向きは季節が配る

**風向き（world の `wind`）は `tailwind`/`crosswind`/`headwind` の3つで、8時間ごとに引き直します。**
候補の重みは季節の段が配るので、乾季は追い風が多く、雨季は向かい風が増えます——これが
「出航に適した季節の窓」（[`GameEndings.md`](../concept/GameEndings.md) 3.1 節）になります。

| 季節 | 追い風 | 横風 | 向かい風 |
|---|---|---|---|
| 穏やか | 30 | 45 | 25 |
| 雨季 | 20 | 40 | 40 |
| 乾季 | 50 | 35 | 15 |

仕組みは天気の遷移（[`ClimateSystem.md`](../engine/ClimateSystem.md) 4 節）と同じ形——残り時間が尽きるたび、
`*_weight` を重みとする `pick` が次を選ぶ——ですが、天気と違って持続は一律です。向きしか無いので、
長さまで振ると何が変わったのか読めなくなります。

筏は風向きを `ancestor` で読みます（外洋には風の概念が無いので、自然に world まで遡ります）。
追い風は +6、横風は +2、向かい風は −3 を `sail_speed` へ寄与します。

### 3.2 海流は外洋が配り、積荷の重さが削る

**海流は外洋（`open_sea`）が `modify: {child: ...}` で、浮かんでいるものすべてへ +3 を配ります。**
筏の側に「今どこの海に居るか」を書かずに済み、別の海を足せば別の海流を持たせられます。

**積荷は `weight` の段が速さを削ります。** 筏の `weight` は中身の重さを含む実効値なので
（[`ContainerSystem.md`](../engine/ContainerSystem.md) 1 節）、積むほど段が上がり、速さが落ちます。

| 積荷 | `sail_speed` への寄与 |
|---|---|
| 80kgまで | 0 |
| 80〜160kg | −1 |
| 160〜240kg | −3 |
| 240kg超 | −6 |

これで「積荷を捨てれば速くなり、速いほど水と食料が保つ」（[`GameEndings.md`](../concept/GameEndings.md) 4 節）が、
専用の宣言なしに成立します。海の上で捨てられる物は、筏から出せばそれだけで軽くなります。

## 4. 到達は、本土へ移ることそのもの

**`voyage_progress` が上限へ達した瞬間、`on_overflow` が筏を本土（`mainland`）へ `move` します。**
プレイヤーも積荷も筏の中に居るので、一緒に着きます。

本土は世界にただ1つ在る場所なので、行き先は型の名前で指せます（`to_object`）。外洋と本土は
どちらも `singleton` で、**世界を作った時点で1つずつ湧きます**（`NewGame.spawnSingletons`）。

着いた後は風も海流も効かない（親が外洋でなくなる）ので、速さは0に戻り、同じ到達が二度起きることは
ありません。

**着いたことを読むのは画面だけで、旗はどこにも持ちません**——死が「もうどこにも居ない」ことで表される
のと同じ分担で（[`VitalsSystem.md`](../engine/VitalsSystem.md) 6 節）、到達は「本土の中に居る」ことが
そのまま表します（`PlayerCharacter.hasReachedMainland`）。画面は、島を出たことを伝えるダイアログを出し、
持ち帰った物を棚へ収め、セーブデータを消して棚の画面へ移ります。

航海中の進みは、現在地の札に出るバー（`voyage_progress` の `gauge`）が示します。**残りの日数は
示しません**（[`GameEndings.md`](../concept/GameEndings.md) 9.3 節の出航前の提示は未実装）。

## 5. 棚の枠は、アーティファクトの型そのもの

**`artifact` タグを持つ型がそのまま棚の枠になります。** 種類を増やすことは、
`artifacts.yaml` へ1つ足すことと同じです。今あるのは金の聖杯だけです。

持ち帰ったと数えるのは、**本土に着いた側に在る物すべて**です。筏の積荷か手持ちかは問いません
——渡り切った側に在ることだけが条件なので、置き場所ごとの数え方を持ちません。

棚は周回そのもの（セーブスロット）とは別の領域に残ります
（[`SaveDataManagement.md`](../engine/SaveDataManagement.md) 棚節）。同じ物を2度持ち帰っても枠は
増えません。棚はタイトル画面からも開けます——空きが見えていることが次の周回へ向かう動機だからです
（[`GameEndings.md`](../concept/GameEndings.md) 6 節）。

## 未決事項・今後の検討課題

- 出航地点による距離の違い（[`GameEndings.md`](../concept/GameEndings.md) 5 節）。距離は今どの浜からでも
  同じで、`voyage_progress` の `range.max` が1つあるだけ。
- 出航前に積載に対する推定日数を示すこと（同 9.3 節）。今は積んでも何も示さない。
- 航海中の時間の送り方。今は既存の休息・睡眠（最長6時間）で送るしかなく、割り込み条件付きの待機
  （同 10 節）は無い。
- 水の運搬。青いヤシの実しか運べる水が無いため、長い航海はかさで頭打ちになる。汲み置きできる容器の
  入手経路（[`LiquidContainerSystem.md`](../engine/LiquidContainerSystem.md)）が要る。
- 航海中の出来事（荒天・浸水・漂流物・釣り）。今は風向きが変わるだけで、海の上に事件が無い。
- 筏の耐久と、櫂のような帆以外の部品。帆は入った（2.1 節）が、構造スロットは1枠しか無い。
- なめし革の帆（+4）。悪い日ほど差が開く伸び方にする余地として、生皮の帆の上を空けてある。
- アーティファクトの総数と、1周回の島に配置する数（[`GameEndings.md`](../concept/GameEndings.md) 6 節）。
  今は金の聖杯1つが、岸壁・岩場・山腹の探索でごく稀に出るだけ。
- アチーブメント（[`GameEndings.md`](../concept/GameEndings.md) 7 節）。棚と同じく周回をまたぐ記録に
  なるが、まだ無い。

# 火システム設計

## 概要

炉を組み、燃料をくべ、火を保ち、その熱を他の物へ渡す仕組みの設計ドキュメントです。「どんな加熱方法が
どう組み立てられ、何に使えるか」までを決めます。

**個別の料理レシピは決めません。** 料理の大半は、既存の `recipes`（[`RecipeSystem.md`](./RecipeSystem.md)）で
未加熱の物を組み立て、最後に火で加熱して完成します。本書が決めるのはその最後の一手の**受け渡し口**
（7 節）だけで、何と何を組み合わせると何になるかは扱いません。火起こし具・燃料になる物の一覧は
[`SurvivalItems.md`](../world/SurvivalItems.md)、火スキルの段は [`Skills.md`](../world/Skills.md) が持ちます。
天気・気温は [`ClimateSystem.md`](./ClimateSystem.md) が、雨から守る `sheltered` は
[`ContainerSystem.md`](./ContainerSystem.md) 6 節が持ちます。

**新設する文法はありません。** 火は炉のプロパティ（1 節）、熱量は燃料が寄与する `modify` の合算（2 節）、
燃料の消費と加熱は `accumulate`（4 節・7 節）、炉の段上げは `on_overflow`（5 節）で、いずれも
[`GameElementDefinition.md`](./GameElementDefinition.md) の既存語彙です。本書が導入するのは語彙の名前
だけです。

| 種別 | 名前 |
|---|---|
| タグ | `hearth`（炉）・`fuel`（燃料）・`tinder`（火口）・`fire_starter`（火起こし具）・`cookware`（火にかける器） |
| スロット | `fire`（火の中）・`cookware`（石の上） |
| プロパティ | `heat`（熱量）・`ember`（熾）・`fuel_remaining`（燃え残り）・`stones`（積んだ石）・`heat_soak`（石の蓄熱）・`cooking_progress`（加熱の進み） |
| 型 | `campfire`（焚き火）・`three_stone_hearth`（三石のかまど）・`stone_hearth`（石囲いの炉）・`hot_stone`（焼け石） |

定義は `public/world-codex/fire.yaml`、検証は `tests/worldCodex/fireYaml.test.ts` に置く予定です。

本ドキュメントは検討結果であり、確定仕様書ではありません。**本書の設計は全体が未実装**で、上記の
ファイルはまだ存在しません。数値はいずれも目安で、`tick` = 15 分・1 日 = 96 tick
（[`DurabilitySystem.md`](./DurabilitySystem.md) 1 節）を前提とします。未決事項は 10 節に整理しています。

## 1. 火は炉のプロパティで、「火」という物は作らない

**燃えていることは炉のプロパティで表し、火そのものを表すオブジェクトは置きません。** 液体が中身の
カードとして独立するのは、注ぎ移せて持ち運べて、量が容器を移っても同じ物だからです
（[`LiquidContainerSystem.md`](./LiquidContainerSystem.md) 1 節）。火はその逆で、炉から取り出せず、
分けられず、別の炉へ移せません。独立させても得るものがなく、カードが 1 枚増えるだけです。

炉は `hearth` タグを持つ設置物（`fixture`）で、次の 2 つのスロットを持ちます。

- **`fire`（火の中）**: 燃料・焼く石・串に刺した物を受ける。段によらずすべての炉が持つ。
- **`cookware`（石の上）**: 火にかける器を受ける。**石を持つ炉だけが持つ**（5 節）。

火にかける場所を火の中と分けるのは、器を支えているのが炎ではなく石だからです。この 2 つが同じ
スロットだと、石を持たない焚き火にも器が載ってしまいます。

## 2. 熱量は、火の中の燃料が寄与するmodifyの合算

**炉の `heat` は実体を持たず、`fire` スロットに入っている燃料が `modify` で寄与した合算です**
（`GameElementDefinition.md` 8.3 節）。装備した防具が着ている者の防御力へ寄与するのと同じ形で、
燃料が親である炉の熱量へ寄与します。

```yaml
# 燃料の側が宣言する
passives:
  - conditions:
      - {in_slot: fire}
      - {object: parent, prop: ember, gt: 0}
    modify:
      parent:
        heat: 2
```

`ember` によるゲート（3 節）が、火が消えている炉に薪を積んでも熱くならないことを表します。

`heat` は段（`stages`、同 6.4 節）を持ち、**何ができるかは段が決めます**。数値の境目を条件の側にも
書くと、境目が 2 箇所に散るためです。

| 段 | `heat` | 状態 | できること |
|---|---|---|---|
| `out` | 0 | 消えている | — |
| `embers` | 1〜4 | 熾火 | 炙る（遅い）、石を焼く、暖をとる |
| `flame` | 5〜14 | 炎 | 焼く、煮る |
| `blaze` | 15〜 | 高温 | 土器の本焼き |

燃料 1 つあたりの寄与と燃え残り（4 節）の目安は次のとおりです。

| 燃料 | `heat` への寄与 | `fuel_remaining` | 燃え尽きるまで（焚き火） |
|---|---|---|---|
| 細い枝 | 2 | 3 | 30 分 |
| 太い枝 | 5 | 10 | 100 分 |
| ヤシの実の皮 | 2 | 2 | 20 分 |

`fire` の枠は炉の段によって 3〜5 なので、**段が上限を決めるのではなく、積める燃料の数が上限を決めます**。
細い枝ばかりでは `flame` に届かず、太い枝を並べて初めて煮炊きができます。

## 3. 熾が、火が生きているかを決める

**燃えているかどうかを表すフラグは持ちません。`ember`（熾）が 0 より大きいことが、火が生きていることです。**

- 着火（6 節）に成功すると `ember` は上限へ張り付きます。
- `heat` が 0 より大きい間、`ember` は減りません（燃えているあいだ熾は絶えない）。
- 燃料が尽きて `heat` が 0 になると、`ember` は毎 tick 減ります。
- `ember` が 0 になると火は死に、次に使うには着火からやり直しです。

```yaml
# 炉の側が宣言する
passives:
  - conditions:
      - {prop: heat, eq: 0}
    accumulate:
      self:
        ember: -1
```

**`ember` が残っているあいだに燃料を足せば、道具なしで火が戻ります。** 火起こし具を使わずに済むことが、
炉を常設する唯一にして最大の利得です。焚き火が「使い捨て」なのは組み方のせいではなく、熾が短時間で
死ぬからで、石で囲った炉が常設になるのは熾が翌朝まで生きるからです。

`ember` の上限は炉ごとに違い、これが段の差の中心になります（5 節）。

`heat` は `ember` を条件に持ち、`ember` の減少は `heat` を条件に持ちますが、循環参照
（`GameElementDefinition.md` 14.1 節）にはあたりません。`heat` は `modify` で導出される実効値、`ember` は
`accumulate` で動く実体値で、実効値の計算が実効値を必要とする関係になっていないためです。

## 4. 燃やすのは炉、熱を出すのは燃料

**燃料が減る速さは炉が持ち、燃料は「どれだけ熱を出すか」だけを持ちます。**

```yaml
# 炉の側が宣言する
passives:
  - conditions:
      - {object: parent, prop: ember, gt: 0}
    accumulate:
      child:
        fuel_remaining: -1.5
```

`child` を対象にしたブロックでは、**ゲートの `self` は子の側**を指します（`GameElementDefinition.md` 8.2 節）。
炉自身の `ember` を見るには `parent` と書きます。

`child` はその炉の子すべてに向きますが、`fuel_remaining` を持つのは燃料だけなので、同じ炉に入っている
石も器も素通りします。

速さを炉が持つことで、**風を切る炉ほど同じ燃料が長くもつ**ことが炉の 1 行だけで表せます。燃料の側は
どの炉にくべられても同じ物のままです。逆にしてしまうと、燃料が「自分がどの炉にいるか」を知らねば
ならなくなります。

`fuel_remaining` は `range: {min: 1, ...}`・`on_shortfall: {destroy: self}` を持ち、0 に達した瞬間に
燃え尽きて消えます（同 6.3 節）。

## 5. 石は火の中ではなく、火のまわりに積む

**石を炉へ運ぶと `stones` が 1 増え、必要数に達した瞬間に次の段の炉へ置き換わります。**

```yaml
# 焚き火の側が宣言する
combinations:
  add_stone:
    with: stone
    destroy: dragged
    add: {self: {stones: 1}}
props:
  stones:
    value: 0
    range: {min: 0, max: 2}
    on_overflow:
      spawn: {object: three_stone_hearth}
      destroy: self
```

`range.max` を必要数より 1 小さく置き、超えた瞬間に反応させるのは、耐久値 0 での自壊と同じ形です
（同 6.3 節）。段上げは箱のレシピにしません。地面に石を運んで積むだけの作業で、材料を箱へ入れて待つ
形にする理由がないためです。

置き換えなので、段を上げると火は消えます（炉を組み直しているため）。

## 6. 炉の段

| 段 | 炉 | 作り方 | `fire` の枠 | `cookware` の枠 | `ember` の上限 | 燃料の消費 |
|---|---|---|---|---|---|---|
| 1 | 焚き火 | 細い枝 3 本 | 3 | なし | 2 tick（30 分） | -1.5/tick |
| 2 | 三石のかまど | 焚き火 ＋ 石 3 | 3 | 1 | 4 tick（1 時間） | -1.5/tick |
| 3 | 石囲いの炉 | 三石のかまど ＋ 石 8 | 5 | 2 | 32 tick（8 時間） | -1.0/tick |

段ごとに増えるものは 1 つずつです。

- **焚き火 → 三石のかまど**: 器を載せる場所ができます。三石が支点になって初めて器を火にかけられる、
  という現実そのままで、器を持たないうちに作っても何も変わりません。
- **三石のかまど → 石囲いの炉**: 熾が翌朝まで生きます。加えて風を切るぶん燃料がもち、火床が広いので
  熾を掻き分けて器を 2 つ置けます。

**器を持たない段階でも煮炊きはできます。** 焼け石を水へ落とす方法（9 節）は容器に耐火性を要求しない
ので、炉の段ではなく石と時間だけで成立します。三石のかまどが効くのは土器を得てからで、この順序は
仕様ではなく「ヤシの殻を直火にかけると焦げる」という事実から出ています。

## 7. 加熱の受け渡しは、子のcooking_progressへのaccumulate

**炉は、火の中にある子の `cooking_progress` を毎 tick 進めます。量は `heat` の段が決めます。**
料理側が書くのは「未加熱の物が `cooking_progress` を持ち、上限に達したら焼き上がりへ置き換わる」だけで、
炉のことも火のことも知りません。

```yaml
# heatのstagesが宣言する
- name: flame
  min: 5
  passives:
    - accumulate:
        child:
          cooking_progress: 3
```

```yaml
# 料理側（本書の対象外）が宣言する
cooking_progress:
  value: 0
  range: {min: 0, max: 24}
  on_overflow:
    spawn: {object: roasted_meat}
    destroy: self
```

これが火と料理の境界です。本書は「熱がどこからどれだけ来るか」までを決め、「何分でどうなるか」は
料理側の `range.max` が持ちます。

**器の中身は、器が自分で加熱します。** 炉の子は器であって、中身は器の子（炉から見れば孫）です。
炉が孫まで手を伸ばすのではなく、器が「炉の上にいるあいだ、自分の中身を加熱する」を自分で持ちます。

```yaml
# 器の側が宣言する
passives:
  - conditions:
      - {object: ancestor, prop: heat, in_stage: flame}
    accumulate:
      child:
        cooking_progress: 3
```

祖先に `heat` が無ければ条件は偽になる（同 14.1 節）ので、**器が炉から降りていることを表す条件は要りません**。
「炉に載っているか」を別に問うと、載っている場所の名前が器の側にも書かれることになります。

## 8. 雨は熾を削る

**雨天で野ざらしの炉は `ember` が余分に減ります。** 消火を判定する専用の仕組みは持たず、火が死ぬ経路を
3 節と同じ 1 本に保ちます。

```yaml
# 炉の側が宣言する
passives:
  - conditions:
      - {object: ancestor, prop: sheltered, eq: 0}
      - {object: ancestor, prop: weather, in: [light_rain, heavy_rain, storm]}
    accumulate:
      self:
        ember: -4
```

熾の上限が段ごとに違う（6 節）ため、同じ雨でも焚き火は一降りで死に、石囲いの炉は生き延びます。屋根の
下に置けば `sheltered` が 1 になって雨の効果自体が消えます（`ContainerSystem.md` 6 節）。

風は独立したプロパティを持ちません。風を切る効果は石囲いの炉の燃料消費（6 節）に、嵐の強風はこの節の
`ember` 減少に畳んであります。

## 9. 火が与えるもの

熱以外に炉が周囲へ与えるものは、いずれも `ancestor`（`GameElementDefinition.md` 8.6 節）への `modify` で
表します。炉が土地に置かれていても小屋の中にあっても、そのプロパティを定義している祖先へ自然に届きます。

- **暖**: `ambient_temperature` を上げます。夜と雨季の冷えに効きます。
- **明かり**: `sunlight` を上げます。夜でも手元の作業ができます。

**焼け石**は物として取り出せる唯一の産物です。`fire` スロットへ入れた石は `heat_soak` を溜め、上限に
達すると `hot_stone` へ置き換わります。炉から出した焼け石は `heat_soak` が減り、0 で普通の石に戻ります。
水を張った器へ落とせば、器に耐火性がなくても中身が沸きます
（[`SurvivalItems.md`](../world/SurvivalItems.md) 4 節）。

## 10. 火スキルとの分担

**炉は「今どれだけ熱いか」を、火スキルは「火を扱えるか」を持ちます**（`Skills.md` 2.2 節）。同じ「低・中・高」
でも見ているものが違います。

| | 決めるもの | 表し方 |
|---|---|---|
| 炉の `heat` の段 | 今この炉で何が作れるか | 燃料の `modify` の合算（2 節） |
| 火スキルの段 | 着火が成功するか、種火を保てるか、高温を出せるか | 着火の `pick` の重み、着火の解放条件 |

着火は火起こし具（`fire_starter`）を炉へ重ねる `combinations` で、`fire` スロットに火口（`tinder`）が
入っていることを要求します。成否は `pick` で、天気とスキルの段が重みを動かします。火口になるのは
細い繊維で、ほぐしたヤシの実の皮（`coconut_husk`）と植物繊維（`plant_fiber`）がそのまま使えます。

## 11. 未決事項・今後の検討課題

- 火が燃え移ること（延焼）。現状は炉の外へ火が出る経路を持たない
- 煙。燻製による保存、虫除け、遠くから見えることのいずれを表すか
- 濡れた燃料。雨ざらしの薪が着火を妨げるか、`fuel_remaining` とは別の軸を持たせるか
- 炉そのものの劣化。雨で崩れるか、放置しても残り続けるか
- `heat` と `ember` をカードのどこに出すか（[`CardView.md`](../ui/CardView.md) のバーは量的オブジェクトの
  中身に紐づくため、そのままは使えない）
- 焼け石の `heat_soak` の減り方が、器の中と外で変わるか（湯へ落とせば急に冷める）
- 段上げで火が消えること（5 節）を、熾だけ引き継ぐ形に変えるかどうか
- 燃料の投入・取り出しにかかる時間（`put_in`、`GameElementDefinition.md` 7.10 節）を持たせるか

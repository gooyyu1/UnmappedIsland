# 火システム設計

## 概要

炉を組み、燃料をくべ、火を保ち、その熱を他の物へ渡す仕組みの設計ドキュメントです。「どんな加熱方法が
どう組み立てられ、何に使えるか」までを決めます。

**個別の料理レシピは決めません。** 料理の大半は、既存の `recipes`（[`RecipeSystem.md`](./RecipeSystem.md)）で
未加熱の物を組み立て、最後に火で加熱して完成します。本書が決めるのはその最後の一手の**受け渡し口**
（7 節）だけで、何と何を組み合わせると何になるかは扱いません。火起こし具・燃料になる物の一覧は
[`SurvivalItems.md`](../world/SurvivalItems.md)、火スキルの段は [`Skills.md`](../world/Skills.md) が持ちます。
天気・気温は [`ClimateSystem.md`](./ClimateSystem.md) が、雨から守る `sheltered` は
[`ContainerSystem.md`](./ContainerSystem.md) 6 節が持ちます。

**新設する文法はありません。** 火は炉のプロパティ（1 節）、くべる操作は `transfer`（2 節）、燃焼と加熱は
`accumulate`（4 節・7 節）、炉の段上げは `on_overflow`（5 節）で、いずれも
[`GameElementDefinition.md`](./GameElementDefinition.md) の既存語彙です。本書が導入するのは語彙の名前
だけです。

| 種別 | 名前 |
|---|---|
| タグ | `hearth`（炉）・`fuel`（燃料）・`fire_starter`（火起こし具）・`cookware`（火にかける器） |
| スロット | `fire`（火にかけているもの） |
| プロパティ | `heat`（火力。燃料の側では「燃やすと出る熱」）・`ember`（熾）・`stones`（積んだ石）・`heat_soak`（石の蓄熱）・`cooking_progress`（加熱の進み） |
| 型 | `campfire`（焚き火）・`three_stone_hearth`（三石のかまど）・`stone_hearth`（石囲いの炉）・`hot_stone`（焼け石） |

定義は `public/world-codex/fire.yaml`、検証は `tests/worldCodex/fireYaml.test.ts` に置く予定です。

本ドキュメントは検討結果であり、確定仕様書ではありません。**本書の設計は全体が未実装**で、上記の
ファイルはまだ存在しません。数値はいずれも目安で、`tick` = 15 分・1 日 = 96 tick
（[`DurabilitySystem.md`](./DurabilitySystem.md) 1 節）を前提とします。未決事項は 11 節に整理しています。

## 1. 火は炉のプロパティで、「火」という物は作らない

**燃えていることは炉のプロパティで表し、火そのものを表すオブジェクトは置きません。** 液体が中身の
カードとして独立するのは、注ぎ移せて持ち運べて、量が容器を移っても同じ物だからです
（[`LiquidContainerSystem.md`](./LiquidContainerSystem.md) 1 節）。火はその逆で、炉から取り出せず、
分けられず、別の炉へ移せません。独立させても得るものがなく、カードが 1 枚増えるだけです。

炉は `hearth` タグを持つ設置物（`fixture`）で、**スロットは `fire`（火にかけているもの）1 つだけ**です。
直火で焼く物も、焼く石も、火にかけた器も、すべてここへ入ります。

**燃料はここへ入りません。** くべた薪は物として炉の中に居続けるのではなく、火力という量へ溶けます
（2 節）。

### 1.1 器の場所は、スロットではなく枠で分ける

**器を置ける炉と置けない炉の差は、`cells`（`GameElementDefinition.md` 7.2 節）の並びが持ちます。**

```yaml
# 三石のかまど
slots:
  fire:
    cells:
      - {accept: {tag: item}}       # 火の中。直火で焼く物・焼く石
      - {accept: {tag: item}}
      - {accept: {tag: item}}
      - {accept: {tag: cookware}}   # 石の上。器だけが載る
```

焚き火はこの最後の枠を持たず、石囲いの炉は 2 つ持ちます（6 節）。器を支えているのが炎ではなく石だと
いうことは、**枠があるかどうか**で表せます。枠は宣言順ではなく型の合う枠へ埋まる（同 7.2 節）ので、
枝を重ねれば火の中へ、器を重ねれば石の上へ、条件を書かずに入ります。

スロットを 2 つに分けないのは、**画面が 1 つの対象につき 1 つの並びしか出せない**ためです。カードを
押したときに開くスロットは `main_item_slot` が 1 つだけ名指しする決まりで（同 7.8 節）、これは画面の
都合というより、「押せば開く」「重ねれば入る」の行き先が 1 つに定まっていることそのものです。2 つ目の
スロットを足すと、どちらを開きどちらへ入れるかを画面側が決めることになり、その規則が YAML の外へ出ます。

## 2. 燃料は物ではなく、炉へ移す量

**薪をくべるのは、水を注ぐのと同じ移送です。** 燃料の側が持っている `heat`（燃やすと出る熱）を、炉の
`heat`（火力）へ `transfer`（`GameElementDefinition.md` 9.5 節）で移し、出し切った燃料は消えます。

```yaml
# 炉の側が宣言する
combinations:
  add_fuel:
    with: fuel
    duration: 5
    transfer:
      amount: 999          # 出せるだけ出す
      from_object: dragged
      from_prop: heat
      to_prop: heat
```

```yaml
# 燃料の側が宣言する
props:
  heat:
    value: 20
    range: {min: 1, max: 20}
    on_shortfall:
      destroy: self        # 出し切った薪は残らない
```

`transfer` は `to` の `range` に収まる分しか動かさないので（同 9.5 節の `allow_overflow: false`）、**炉が
一杯なら入るぶんだけ入り、残りは薪に留まって手元へ戻ります**。液体を注ぎ移すときと同じ振る舞いで、
燃料を無駄にしません。

### 2.1 燃料を枠へ入れない理由

くべた薪を `fire` の枠に置き続ける形も書けますが、採りません。

- **1 つの枠には同じ型しか積めない**（同 7.6 節）ので、枠の数がそのまま同時に燃やせる燃料の種類数に
  なります。小枝と太い枝と皮を一緒にくべる、という当たり前のことが枠の都合で縛られます。
- **燃えている炉から薪を取り出せてしまいます。** 半分燃えた薪も型は同じままなので、見た目に区別が
  つかないうえ、炉から出した瞬間に燃えるのが止まります。
- **枠が燃料に食われます。** `fire` の枠は焼く物と焼く石のためのもので、薪が入ると小さい炉ほど
  何も焼けなくなります。

量へ溶かせば 3 つとも消えます。**燃料は炉の中に「在る」ものではなく、火が「食べてしまった」もの**
だからです。

### 2.2 火力の段

`heat` は段（`stages`、同 6.4 節）を持ち、**段が決めるのは加熱の速さです**（7 節）。数値の境目を条件の
側にも書くと、境目が 2 箇所に散るためです。

| 段 | `heat` | 状態 | 加熱の速さ | 火力の減り |
|---|---|---|---|---|
| `out` | 0 | 消えている | 0 | — |
| `embers` | 1〜9 | 熾火 | 1/tick | -0.5/tick |
| `flame` | 10〜39 | 炎 | 3/tick | -1.5/tick |
| `blaze` | 40〜 | 高温 | 5/tick | -4/tick |

**火力は残りの燃料そのものです。** くべれば上がり、燃えるにつれ下がり、段を跨いで落ちていきます。
大きい火ほど速く食うので、`blaze` を保つには薪を注ぎ続けることになります。

**焼く・煮るに段の下限は設けません。** 熾火の上でも湯は沸きます——むしろ煮炊きは熾火のほうが安定
するので、鍋を炎に載せる必要はありません。炎が速いだけで、できることが増えるわけではないためです。
段を下限として要求するのは、土器の本焼き（`blaze`）だけです。

燃料 1 つが持つ `heat` の目安は次のとおりです。

| 燃料 | `heat` | 焚き火へ 1 つくべたとき |
|---|---|---|
| 小枝 | 3 | 熾火が 30 分 |
| 太い枝 | 20 | 炎が 100 分、その後 熾火が 5 時間 |
| ヤシの実の皮 | 2 | 熾火が 20 分 |

**`heat` の上限は炉が持ちます**（6 節）。焚き火にどれだけ薪を積んでも `blaze` に届かないのは、開いた
焚き火にそれだけの薪を積めないからで、段の側に別の禁止を書く必要はありません。

## 3. 熾が、火が生きているかを決める

**燃えているかどうかを表すフラグは持ちません。`ember`（熾）が 0 より大きいことが、火が生きていることです。**

- 着火（6 節）に成功すると `ember` は上限へ張り付きます。
- `heat` が 0 より大きい間、`ember` は減りません（燃えているあいだ熾は絶えない）。
- 燃料が尽きて `heat` が 0 になると、`ember` は毎 tick 減ります。
- `ember` が 0 になると火は死に、次に使うには着火からやり直しです。

```yaml
# 炉の側が宣言する
passives:
  - conditions:
      - {prop: heat, eq: 0}
    accumulate:
      self:
        ember: -1
```

**`ember` が残っているあいだに燃料を足せば、道具なしで火が戻ります。** 火起こし具を使わずに済むことが、
炉を常設する唯一にして最大の利得です。焚き火が「使い捨て」なのは組み方のせいではなく、熾が短時間で
死ぬからで、石で囲った炉が常設になるのは熾が翌朝まで生きるからです。

`ember` の上限は炉ごとに違い、これが段の差の中心になります（5 節）。

`heat` の減少は `ember` を条件に持ち、`ember` の減少は `heat` を条件に持ちますが、循環参照
（`GameElementDefinition.md` 14.1 節）にはあたりません。禁じられているのは**実効値の計算が自分の
実効値に依存すること**で、`heat` も `ember` も `modify` を持たない実体値だからです。互いを見ているのは
`accumulate` のゲートで、これは実効値の計算に入りません。

## 4. 燃える速さは、炉と段が決める

**火力が減る速さは炉が持ちます。** 燃料の側は、燃やすとどれだけの熱が出るか（2 節）だけを持ち、
どの炉にくべられても同じ物のままです。

```yaml
# 炉のheatのstagesが宣言する
- name: flame
  min: 10
  passives:
    - conditions:
        - {prop: ember, gt: 0}
      accumulate:
        self:
          heat: -1.5
```

段ごとに書くのは、**大きい火ほど速く燃料を食う**ためです（2.2 節の表）。同じ段でも石囲いの炉は
遅く、これが**風を切る炉ほど同じ薪が長くもつ**ことにあたります（6 節）。

`ember` によるゲート（3 節）は、**火が死んでいる炉に積んだ薪が勝手に減らない**ことを表します。組んで
おいた薪はそのまま残り、着火した瞬間からそこを食い始めます。

## 5. 石は火の中ではなく、火のまわりに積む

**石を炉へ運ぶと `stones` が 1 増え、必要数に達した瞬間に次の段の炉へ置き換わります。**

```yaml
# 焚き火の側が宣言する
combinations:
  add_stone:
    with: stone
    destroy: dragged
    add: {self: {stones: 1}}
props:
  stones:
    value: 0
    range: {min: 0, max: 2}
    on_overflow:
      spawn: {object: three_stone_hearth}
      destroy: self
```

`range.max` を必要数より 1 小さく置き、超えた瞬間に反応させるのは、耐久値 0 での自壊と同じ形です
（同 6.3 節）。段上げは箱のレシピにしません。地面に石を運んで積むだけの作業で、材料を箱へ入れて待つ
形にする理由がないためです。

置き換えなので、段を上げると火は消えます（炉を組み直しているため）。

### 5.1 石は既存の `stone` をそのまま使い、炉は一人用の大きさになる

**専用の大きな石は導入しません。** 既存の `stone`（1kg・手に持てる大きさ、`locations.yaml`）を運んで積みます。
石囲いの炉まで組んでも石は 11 個・11kg で、持てる量（33kg）の 3 分の 1 に収まるので、運搬そのものが
関門にはなりません。

その結果、炉は一般に想起されるかまどよりだいぶ小ぶりな、**一人分の器が 1 つ載る大きさ**になります。
これは制約ではなく、この島に居るのが 1 人だという事実の現れです。

三石が器を持ち上げる高さも 10cm 弱なので、器の下に入るのは炎ではなく熾です。2 節が焼く・煮るに段の
下限を設けないのは、この寸法とも噛み合っています。

石囲いの炉の熾が翌朝まで生きるのも、この大きさで成立します。熾が長く保つ主因は灰に埋めて空気の
流れを絞ることで、石が担うのは風防と輻射だからです。石の熱容量に頼る設計なら、この大きさでは足りません。

## 6. 炉の段

| 段 | 炉 | 作り方 | 火の中の枠 | 器の枠 | `heat` の上限 | `ember` の上限 | 炎での火力の減り |
|---|---|---|---|---|---|---|---|
| 1 | 焚き火 | 小枝 3 本 | 2 | なし | 30 | 2 tick（30 分） | -1.5/tick |
| 2 | 三石のかまど | 焚き火 ＋ 石 3 | 2 | 1 | 30 | 4 tick（1 時間） | -1.5/tick |
| 3 | 石囲いの炉 | 三石のかまど ＋ 石 8 | 3 | 2 | 120 | 32 tick（8 時間） | -1.0/tick |

どちらの枠も同じ `fire` スロットの `cells` に並びます（1.1 節）。枠は焼く物と焼く石のためのもので、
薪は枠を使いません（2.1 節）。

段ごとに増えるものは 1 つずつです。

- **焚き火 → 三石のかまど**: 器を載せる場所ができます。三石が支点になって初めて器を火にかけられる、
  という現実そのままで、器を持たないうちに作っても何も変わりません。
- **三石のかまど → 石囲いの炉**: 熾が翌朝まで生きます。加えて積める薪が 4 倍になって `blaze` に届き、
  風を切るぶん同じ薪が長くもち、火床が広いので熾を掻き分けて器を 2 つ置けます。

**土器の本焼き（`blaze`）に届くのは石囲いの炉だけ**ですが、それは段に禁止を書いた結果ではなく、
`heat` の上限が 30 と 120 で違うことの帰結です（2.2 節）。

**器を持たない段階でも煮炊きはできます。** 焼け石を水へ落とす方法（9 節）は容器に耐火性を要求しない
ので、炉の段ではなく石と時間だけで成立します。三石のかまどが効くのは土器を得てからで、この順序は
仕様ではなく「ヤシの殻を直火にかけると焦げる」という事実から出ています。

## 7. 加熱の受け渡しは、子のcooking_progressへのaccumulate

**炉は、火の中にある子の `cooking_progress` を毎 tick 進めます。量は `heat` の段が決めます。**
料理側が書くのは「未加熱の物が `cooking_progress` を持ち、上限に達したら焼き上がりへ置き換わる」だけで、
炉のことも火のことも知りません。

```yaml
# 炉のheatのstagesが宣言する（4節の火力の減りと同じブロックに並ぶ）
- name: flame
  min: 10
  passives:
    - accumulate:
        child:
          cooking_progress: 3
```

```yaml
# 料理側（本書の対象外）が宣言する
cooking_progress:
  value: 0
  range: {min: 0, max: 24}
  on_overflow:
    spawn: {object: roasted_meat}
    destroy: self
```

これが火と料理の境界です。本書は「熱がどこからどれだけ来るか」までを決め、「何分でどうなるか」は
料理側の `range.max` が持ちます。

**器の中身は、器が自分で加熱します。** 炉の子は器であって、中身は器の子（炉から見れば孫）です。
炉が孫まで手を伸ばすのではなく、器が「炉の上にいるあいだ、自分の中身を加熱する」を自分で持ちます。

```yaml
# 器の側が宣言する
passives:
  - conditions:
      - {object: ancestor, prop: heat, in_stage: embers}
    accumulate:
      child:
        cooking_progress: 1
  - conditions:
      - {object: ancestor, prop: heat, in_stage: flame}
    accumulate:
      child:
        cooking_progress: 3
```

器は段ごとに 1 ブロックを並べます（同 8.5 節）。炉の側と同じ速さを 2 箇所に書くことになるので、
値の出所は 2 節の表 1 つに保ちます。

祖先に `heat` が無ければ条件は偽になる（同 14.1 節）ので、**器が炉から降りていることを表す条件は要りません**。
「炉に載っているか」を別に問うと、載っている場所の名前が器の側にも書かれることになります。

### 7.1 直火で焼く物は、そのまま火の中へ入れる

**串のような、焼くためだけの中間の物は挟みません。** 肉はそのまま火の中の枠へ入れます。灰に埋めて
焼くのは実在する最も原始的な焼き方で、道具を何も持たない段階と噛み合います。

串を挟むと、型と工程が 1 つずつ増えるのに**プレイヤーの選択は増えません**。串の材料になる小枝は
いくらでも拾えるので、持っているなら必ず刺すことになるからです。

**見張る理由は、串ではなく焼きすぎが作ります。** `cooking_progress` の上限で焼き上がりへ置き換わった後、
その焼き上がり自身がまた `cooking_progress` を持てば、放っておくと焦げます。串が担うはずだった「火の
そばに居る理由」は、料理側の型が 2 段重なるだけで出ます。

**一度に焼ける量は枠の数が決めます。** 焚き火の枠は 2 つで、石囲いの炉でも 3 つです。薪は枠を
使わない（2.1 節）ので、この数はそのまま「火のそばに並べられる物の数」になります。

串を後から入れるなら、中間素材としてではなく、**1 つの枠に何本も刺せる道具**として入れます。器と
同じ形（`cookware` の枠に入り、自分の子を加熱する）で書けるので、この節の構造は変わりません。

## 8. 雨は熾を削る

**雨天で野ざらしの炉は `ember` が余分に減ります。** 消火を判定する専用の仕組みは持たず、火が死ぬ経路を
3 節と同じ 1 本に保ちます。

```yaml
# 炉の側が宣言する
passives:
  - conditions:
      - {object: ancestor, prop: sheltered, eq: 0}
      - {object: ancestor, prop: weather, in: [light_rain, heavy_rain, storm]}
    accumulate:
      self:
        ember: -4
```

熾の上限が段ごとに違う（6 節）ため、同じ雨でも焚き火は一降りで死に、石囲いの炉は生き延びます。屋根の
下に置けば `sheltered` が 1 になって雨の効果自体が消えます（`ContainerSystem.md` 6 節）。

風は独立したプロパティを持ちません。風を切る効果は石囲いの炉の燃料消費（6 節）に、嵐の強風はこの節の
`ember` 減少に畳んであります。

## 9. 火が与えるもの

熱以外に炉が周囲へ与えるものは、いずれも `ancestor`（`GameElementDefinition.md` 8.6 節）への `modify` で
表します。炉が土地に置かれていても小屋の中にあっても、そのプロパティを定義している祖先へ自然に届きます。

- **暖**: `ambient_temperature` を上げます。夜と雨季の冷えに効きます。
- **明かり**: `sunlight` を上げます。夜でも手元の作業ができます。

**焼け石**は物として取り出せる唯一の産物です。`fire` スロットへ入れた石は `heat_soak` を溜め、上限に
達すると `hot_stone` へ置き換わります。炉から出した焼け石は `heat_soak` が減り、0 で普通の石に戻ります。
水を張った器へ落とせば、器に耐火性がなくても中身が沸きます
（[`SurvivalItems.md`](../world/SurvivalItems.md) 4 節）。

## 10. 火スキルとの分担

**炉は「今どれだけ熱いか」を、火スキルは「火を扱えるか」を持ちます**（`Skills.md` 2.2 節）。同じ「低・中・高」
でも見ているものが違います。

| | 決めるもの | 表し方 |
|---|---|---|
| 炉の `heat` の段 | 今この炉がどれだけ速く加熱するか | くべた燃料から移した火力（2 節） |
| 火スキルの段 | 着火が成功するか、種火を保てるか、高温を出せるか | 着火の `pick` の重み、着火の解放条件 |

着火は火起こし具（`fire_starter`）を炉へ重ねる `combinations` で、**`heat` が 0 より大きいこと**、
つまり薪が組んであることだけを要求します。成功すれば `ember` が上限へ張り付き、そこから燃え始めます
（3 節）。成否は `pick` で、天気とスキルの段が重みを動かします。

火口（よく乾いた小枝・ほぐしたヤシの実の皮・植物繊維）は、くべれば燃料として `heat` になります。
着火の必須条件として別に持たせるかどうかは未決です（11 節）。

## 11. 未決事項・今後の検討課題

- 火が燃え移ること（延焼）。現状は炉の外へ火が出る経路を持たない
- 煙。燻製による保存、虫除け、遠くから見えることのいずれを表すか
- 火口を、着火の必須条件として別に持たせるか（10 節）。燃料が量へ溶ける以上、「火口が入っている」を
  言うには炉の側にもう 1 つプロパティが要る。着火の成功率だけを動かす形で足りるかどうか
- 濡れた燃料。雨ざらしの薪が着火を妨げるか、燃料の `heat` を減らす形で表すか
- 炉そのものの劣化。雨で崩れるか、放置しても残り続けるか
- `heat` と `ember` をカードのどこに出すか（[`CardView.md`](../ui/CardView.md) のバーは量的オブジェクトの
  中身に紐づくため、そのままは使えない）
- 器の枠（1.1 節）に「ここは器を置く枠」と見せるかどうか。空き枠へ受け入れる型を薄く敷く仕組み
  （`EmptyCard`）は既にあるが、枠ごとの受け入れ型を画面へ渡しているのは製作中オブジェクトの材料欄
  だけで、普通の入れ物は `SlotDef` の枠ごとの `accept` を見ていない。受け入れがタグの場合に何の絵を
  出すかも決まっていない
- 焼け石の `heat_soak` の減り方が、器の中と外で変わるか（湯へ落とせば急に冷める）
- 段上げで火が消えること（5 節）を、熾だけ引き継ぐ形に変えるかどうか
- 燃料の投入・取り出しにかかる時間（`put_in`、`GameElementDefinition.md` 7.10 節）を持たせるか
- 土器の本焼き（`blaze`）が、石囲いの炉で届くのか、炉とは別の閉じた窯を要るのか。窯にするなら、
  5.1 節が退けた大きな石を導入する最初の理由になる

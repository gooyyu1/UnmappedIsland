/**
 * 警戒を知らせる明滅の、片道の時間（ミリ秒）。
 *
 * **明滅の速さは1つだけ**にする。カードの輪郭（Card）も、ステータスのバーの枠（ProgressBar）も、
 * 画面全体の枠（ScreenAlertFrame）も同じ速さ。別々に持つと、同じ1つの警戒が3つの拍で点滅して、
 * 別々の出来事を知らせているように見える。
 *
 * **最も薄いときの濃さは各々が持つ。** 面積が違えば同じ濃さでも目に入る量が違うので、そちらは
 * 揃える意味が無い（画面全体の枠だけ薄い）。
 */
export const ALERT_BLINK_MS = 450;

/**
 * 縦に1件ずつ並べる一覧の画面（テスト用シナリオの選択・設定）が共有する余白（u単位）。
 *
 * **2つの画面は同じ形として読まれる**ので、余白が食い違うと、同じ操作をしているのに別の画面へ
 * 来たように見える。1件ぶんの高さは中身の量で決まるため、各画面が持つ。
 */
export const LIST_PADDING = 20;
export const LIST_ITEM_PADDING_X = 24;

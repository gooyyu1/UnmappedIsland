# 元衛生兵。どの数値も基準どおりで、他のキャラクタはこの体を基準にどこを削ってどこを伸ばすかで
# 表される。手当ての心得を活かせる治療の仕組みはまだ無いため、個性は体力の高さだけに出る。
# 各プロパティの意味・値の刻み方は docs/world/Characters.md 参照。

object_defs:
  medic:
    singleton: true
    traits: [player_character]
    slots:
      hand: {cell_count: 6}
    props:
      # 満腹から1日（96 tick）。
      satiety:
        tags: [status, nutrition]
        value: 72
        range: {min: 0, max: 96}
        stages:
          - {name: starving, alert: danger}
          - {name: hungry, min: 12, alert: caution}
          - {name: peckish, min: 48, alert: watch}
          - {name: fed, min: 77}
        passives:
          - accumulate:
              self:
                satiety: -1
      # 満水から3日（288 tick）。
      hydration:
        tags: [status, nutrition]
        value: 216
        range: {min: 0, max: 288}
        stages:
          - {name: dehydrated, alert: fatal}
          - {name: parched, min: 24, alert: danger}
          - {name: thirsty, min: 96, alert: caution}
          - {name: dryish, min: 192, alert: watch}
          - {name: hydrated, min: 230}
          - {name: full, min: 288}
        passives:
          - accumulate:
              self:
                hydration: -1
      # 絶食に耐えられるのは、最大限に肥えた状態から60日（5760 tick）。
      body_fat:
        tags: [nutrition]
        value: 1440
        range: {min: 0, max: 5760}
        passives:
          - accumulate:
              self:
                body_fat: -1
      # 眠らずにいられるのは24時間（96 tick）。
      wakefulness:
        tags: [status, health]
        value: 96
        range: {min: 0, max: 96}
        stages:
          - {name: overtired, alert: danger}
          - {name: sleepy, min: 12, alert: caution}
          - {name: yawning, min: 48, alert: watch}
          - {name: rested, min: 77}
        passives:
          - accumulate:
              self:
                wakefulness: -1
      # 現場を歩き回って培った、基準より1割高い体力。
      stamina:
        tags: [status, health]
        value: 110
        range: {min: 0, max: 110}
        stages:
          - {name: exhausted, alert: danger}
          - {name: tired, min: 22, alert: caution}
          - {name: winded, min: 66, alert: watch}
          - {name: fresh, min: 88}
      # 担架を運び慣れた、基準より1割多い持てる量。持ち物の重さ（g）は中身から導出されるので、valueは0のまま
      # （ContainerSystem.md 2節）。増える側が悪いプロパティなので、段はmaxからの割合で刻む
      # （docs/world/Characters.md）。
      load:
        tags: [status, health]
        value: 0
        range: {min: 0, max: 33000}
        stages:
          - {name: light}
          - {name: laden, min: 8250, alert: watch}
          - {name: heavy, min: 16500, alert: caution}
          - {name: too_heavy, min: 27500, alert: danger}

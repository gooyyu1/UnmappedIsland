# 実装スタイル

TypeScriptのコーディング規約は [`docs/CodingConventions.md`](docs/CodingConventions.md)
に従う。変更後は `npm run lint` / `npm run typecheck` / `npm test` / `npm run format:check` が
すべて成功することを確認する（CIが走らせるのはこの4つ）。

整形のずれは `npm run lint` では検出できない（eslintの整形系ルールはeslint-config-prettierが
無効化しており、prettierだけが見ている）。編集ツールで書いたファイルは自動整形フックが直すが、
フックが効かない経路で編集した場合は `npm run format` で直す。`.prettierrc` の
`"endOfLine": "auto"` により、**CRLFの作業ツリーでも `format:check` は通る**。省く理由は無い。

## 公開サイト（GitHub Pages）

`.github/workflows/pages.yml` が `main` へのプッシュのたびにサイトを丸ごと作り直してデプロイする。
**生成物はリポジトリに存在しない**（出力先の `site/` は `.gitignore` 済み）。

原稿は `docs/` に置く。サイトは3本立てで、それぞれ出どころが違う。

| サイトのパス | 出どころ |
|---|---|
| `/docs/` | `docs/**/*.md` を Pandoc で HTML 化。`*.html`・画像はそのままコピー |
| `/reference/` | `src/` から TypeDoc が生成 |
| `/game/` | Vite がビルドしたゲーム本体 |

**ドキュメント類は `docs/` 以下を編集すること。** ローカルに `site/` を作っても公開内容には
影響しない。

## Phaserの画面をスクリーンショットで確認する

[`run` skill](.claude/skills/run/SKILL.md) を使うこと。このプロジェクトは
`playwright`/`@playwright/test` に依存しておらず素朴な起動方法では失敗する、という落とし穴と
その回避手順（スクリプト付き）をskill側にまとめてある。

# 修正作業の進め方

## 1. 再現手順を最初にテストへ落とす

issueに再現手順が書いてあるなら、コードを読む前にそれをそのままテストにして、実際に失敗させる。
このリポジトリのバグは実データ（`src/assets/world-codex/**.yaml`）と実シナリオを通して初めて
再現するものが多い。合成YAMLで組んだ最小ケースは、書いた本人の仮説を確認するだけで、issueの
現象とは別物になりうる。

再現しなかったら、そこで止まって報告する。「原因はこれだろう」で書き始めない。
直したあとは同じテストが通ることで直ったと言えるようにする。

## 2. 原因の特定は最深に、変更は最小に

「最小変更」は差分の大きさの話であって、原因究明の深さの話ではない。症状が出ている行を止める
のではなく、不変条件（invariant）がどこで破れたかまで降りてから、そこを最小の差分で直す。

原因が「ある不変条件を、それを持つべきクラスではなく呼び出し側が肩代わりしていたこと」に
行き着くことは多い。その場合は不変条件を持つべき側へ移すのが正しい修正で、呼び出し側での
場当たり的な分岐追加は誤り（「自分のことは自分でする」参照）。

## 3. 同じ誤りの兄弟を必ず探す

原因が特定できたら、同じパターンが他にないかリポジトリ全体をgrepしてから直す。
YAMLの定義誤りなら同じファイル・同じ種類の定義を、コードなら同じ役割を持つ他の経路を見る。
issueに書かれた1箇所だけを直して、同じ原因の残りを放置しないこと。

**掃いた形には、再発したときに落ちるものを同じPRで置く**（下の「置いた主張は、破れたときに落ちる
ものと対で置く」と同じ理由）。置かないと、次に同じ形が生えたことに誰も気づかず、**レビューのスメル
→ 新しい issue → 別のセッション**という周回で、1つの形が issue の数だけセッションを食う。
issue の側でそれを止める書き方は
[`.claude/parallel-work.md`](.claude/parallel-work.md)「タスク issue の型」。

**自分で立てた issue には `origin:agent` と `goal:`（向かう先）を付ける**（起票のときに一緒に渡す）。
前者は人が立てた issue と見分けるための印で、人とエージェントは同じアカウントで書き込むため
**付けられるのは立てた本人だけ**。後者は**盤面が配る順と、発掘係を立てるかを決める印**で、向かう先を
知っているのも立てた本人だけ——[`.claude/parallel-work.md`](.claude/parallel-work.md)「自分で立てた
issue には `origin:agent` を付ける」と「向かう先（`goal:`）も名乗らせる」。

範囲外だと判断して直さない場合は、見つけたことと直さない理由を残す。黙って落とさない。
書く先は2つに分かれる。**このPRの後に誰かが手を動かすなら issue を立てる**（番号はPR本文へ書き写さ
ない。issue が在れば盤面が配る）、**立てずに済ませるなら `## 自己点検` の「同じ誤りの兄弟」**
（記録だけで、誰も手を動かさない）。

## 4. 直したことで不要になった旧コードを消す

修正が入ったせいで意味を失った分岐・回避策・特別扱いは、同じPRで消してテストが通ることを
確認する。残すと、次に同じ場所を触る人が「両方に意味がある」と誤読する。

## 5. レビュー指摘は「その行の直し方」ではなく「原則」として受け取る

レビューで方針を指摘されたら、指摘された行を書き換えて終わりにしない。その指摘が原則を
述べているなら、同じ原則に反している箇所を洗い出して一緒に直す（3節と同じ）。
指摘の意図が読み取れないときは、推測で書き換えずに質問する。

## 6. PR本文は最後に、実際のdiffから書く

PR本文は「やるつもりのこと」ではなく「実際に入った変更」を書く場所。最終diffを読み直してから
書く。

- 実在しないコードの引用（「イメージ」「conceptual」等）を載せない。載せるコードは実際の
  差分から引く。
- 方針が途中で変わったら本文を書き換える。コメントでの補足だけでは、マージ後に残る記録が
  誤ったままになる。
- タイトル・本文は日本語で、変更の理由（何が壊れていたか）と結果を簡潔に書く。

## 7. 手を付けないPRを残さない

差分が空のまま、あるいは途中で捨てた作業のPRは、開いたままにせずクローズする。

## 置いた主張は、破れたときに落ちるものと対で置く

コメントに書いた契約・不変条件、仕様書の宣言、doc コメント——**新しく主張を置いたなら、それが破れた
ときに落ちるものを同じPRで置く。** 置いた時点では本当のことでも、次にそこを触る人は主張を守るものが
在るかを知らないので、破れても誰も気づかないまま、書いてあることと現物がずれていく。

**push の前に、差分が新しく置いた主張を洗い出し、1つずつ「これを破ると何が落ちるか」を答える。**
答えられないものは、落ちるものが無い。

「落ちる」と言えるのは、次のすべてを満たすときだけ。1つでも欠けていれば無いのと同じ。

- **その主張の面を見ている。** 別の面を見ている検査は、主張を破っても緑のまま
  （生成物の丸ごと比較で通す、値が動かない側の入力しか渡さない）。
- **必ず走る。** 環境の有無で飛ぶ検査（`describe.runIf(...)` や、道具が無ければ黙って抜ける経路）は、
  「通った」と「走っていない」が緑では区別できない。
- **落ちたことが誰かへ届く。** 印や文字列を出すだけで読む者が居ない出力は、破れても何も起きない。

効いていることの示し方は [`.claude/policies.md`](.claude/policies.md)「見張りが効いていることの
示し方」——**実際に主張を破って赤くなるのを見てから**報告する。

**落ちるものを置けないなら、主張のほうを見直す。** コードを読めば分かることの言い換えなら消す
（下の「ドキュメント・コメントのスタイル」）。消せない決めごとなら、置けない理由を `## 仮決め` へ
1行書く——そこはセッションから人間へ届く場所で、レビュアーが人へ回す。

## 自分のことは自分でする（カプセル化の徹底）

あるクラスの内部状態（値・不変条件）に関する処理は、そのクラス自身が行う。呼び出し側（他のクラス）が
「値を変更した後にこれとこれを判定する」といった手順を代わりに引き受けてはいけない。

- 悪い例: `WorldObject` が `PropertyValue` の値を変更した後、`WorldObject` 側で
  `checkRangeEvents` を呼んで range 超過を判定する。
- 良い例: `PropertyValue.add`/`setNumber` 自身が、値を変更した直後に自分の `checkRangeEvents` を
  呼ぶ。呼び出し側は「値を変えてほしい」と依頼するだけで、変更後に何を判定すべきかは一切知らない。

判断基準: あるクラスの公開メソッドを呼んだ後、呼び出し側が「この後、あのメソッドも
呼ばないと壊れる」という手順を覚えておく必要があるなら、その手順は呼ばれる側のクラス自身に
移すべきサイン。

## リファクタリングは「1つの仕組みで100%」から始める

まず**全部が1つの仕組みに乗る**と仮定し、そのうえで「差をどうパラメータとして表すか」を考える。
差があることを理由に仕組みを分けた時点で、リファクタリングの意味は失われる。

- **既存実装の差は仕様ではない。** 場当たりに積み上がった差を「種類」として受け取り、種類ごとに
  名前を与えるのは、構造ではなく命名を変えただけ。カードのバーで、色の決め方が4系統あることを
  4種類のバーだと解釈してタグを3つ作った例がある——実際の差は「rangeのどちらの端が良いか」の
  1点だけで、列挙型2つで全部が畳めた。
- **「色が違う」「向きが違う」は分ける理由にならない。** 差は、分けられないと**証明できるまで**
  パラメータとして扱う。分けるなら「パラメータ化した単一の仕組みでは表せない具体例」を1つ挙げる。
- **数えて確かめる。** 提案した仕組みの数が既存のケース数とほぼ同じなら、何も畳めていない。
- **既存の挙動を保つかは、最初に確認して全体へ一律に適用する。** 一箇所でも変えるなら、
  それは制約ではない。統合を阻む箇所にだけ持ち出すのは、結論の後付けになる。
- 立てる問いを間違えない。「**それぞれを今のまま動かすには、どう分けるか**」を問う限り、仕組みの数は
  既存の挙動の数と必ず一致する。問うべきは「**これらを分けている差は、突き詰めると何か**」。

# 調査とレビューの進め方

## 名前と中身が、今の機能の現実と一致しているかまで見る

レビューは構造（Tell Don't Ask・単一責務）だけで止めない。**フィールドや型の意味が、今の機能の現実から
ずれていないか**を疑う。挙動を保存するリファクタは古い意味を忠実に移植するので、機能追加で現実が
変わった箇所に不具合が潜む（単数の `released` が、複数枚を運べるようになった後も「掴んだ1つ」しか
運んでいなかった例）。

暗黙の位置規約（先頭が特別、など）は名前付きフィールドへ直す。2箇所が暗黙に一致すべき規約は1箇所へ
集める。プログラミング上の都合だけで存在するオブジェクトや、つじつま合わせの計算は排除する。

## 退行は、原因を推論する前に「いつ壊れたか」を特定する

「前は動いていた」と言われたら、症状が最もはっきり出るケースを先に決め、**疑う変更より前**のコミットまで
戻って比較する。差分が出ないなら、原因ではなく**基準の選び方**を疑う。自分の過去の変更も等しく容疑者に
入れる——要約を経た文脈では、自分の書いたコードほど「決着済みの背景」として無意識に除外しやすい。

混入時期を報告者が知っているとは限らない。「いつ」を出すのは自分の仕事。

## 調べるための差し替えは、ベタ塗りにしない

素材を差し替えて描画を調べるときは、**格子模様＋四隅に別々の色の目印＋外周の線**を入れた画像を使う。
倍率・切り出し位置・向き・欠けが1枚で分かる。ベタ塗りは「使われている」しか教えないので、自分の仮説を
否定できず、見当違いな推論を積み上げることになる。

プローブは「見えるか」ではなく「**何が違うと違って見えるか**」で選ぶ。見た目を見る前に数値を1つ取れる
なら、そのほうが速い。

# ドキュメント・コメントのスタイル

読み手は人間。簡潔に書く。`docs/` 配下の構造（見出し・節番号・概要の定型・実装状況と確定度の表記・
参照の書き方）は [`docs/DocumentStyle.md`](docs/DocumentStyle.md) に従う。

**`docs/` の記述は、確定の宣言が無ければ暫定。** 宣言は2つの形がある——節の見出しに付く `【確定】` と、
文書まるごとが土台のときに題名の直後で1回宣言する `**本書は全体が確定です。**`（`GameConcept.md` が
この形）。**どちらかの射程に入る記述だけ**が「覆すには人間の判断が要る」で、それ以外は従ってよいが
覆す提案を出してよい。決まっていない箇所に当たったら、止まらずに仮決めを書いて進む（宣言は付けない）。
詳細は [`docs/DocumentStyle.md`](docs/DocumentStyle.md) 6節。

- 各箇所には、その箇所の読者に必要なことだけを書く。同じ説明を複数箇所に書かない。
  詳細は責務を持つ一箇所（定義側のクラス・ドキュメントの該当節）にだけ書き、他からは短い参照に留める。
- コメントに書いてよいのは、コードから読み取れない契約・不変条件・制約と、非自明な設計理由（1〜2文）、
  および仕様書の節番号への参照だけ。コードの動作をなぞる説明は書かない。
- 経緯（旧仕様、変更履歴、不採用案、検討過程）はコメントにもドキュメント本文にも書かない。
  過去の失敗を繰り返さないために残す価値がある事項だけを
  [`docs/engine/DesignNotes.md`](docs/engine/DesignNotes.md) に集約する。
  **この禁止が掛からないのは、経緯そのものを主題とする文書だけ**——どの文書がそれかと、その中での
  書き方は [`docs/DocumentStyle.md`](docs/DocumentStyle.md) 9.1節。

## 数え上げを書かない

**他の場所を数えれば出る値を、文章にもコメントにも書かない。** 「向かう先は3つ」「7項目の箇条書き」
「6分割」のような記述は、**対象が1つ増減した瞬間に嘘になる**うえ、対象を触る全員が同じ行を書き換える
ので、**並列に走る変更どうしを必ずコンフリクトさせる。** 数はその場で数えれば分かるので、書いても
読み手は何も得ない。禁止する。

- **全部を指すなら「すべて」と書く。** 「3つの向かう先」ではなく「向かう先」「どの向かう先も」。
- **一部を指すなら、数ではなく条件を書く。** 「7項目のうち2つ」ではなく「〜であるもの」。
  条件で書けば、対象が増えても記述は正しいままで、書き換えも要らない。
- 数え上げは個数だけではない。**列挙そのもの**（対象の側に在るものを別の場所へ書き写した一覧）も
  同じ理由で書かない。指すなら在り処を指す。
- 節番号・段の番号のように、**順序や対応を指すための番号**は数え上げではない。
書いてよいのは次のどちらか。

- **その数そのものが仕様であるとき**（「手札は5枚」のように、数が変わることが仕様の変更になるもの）。
  **実装や別の文書を数えれば出る値は、仕様ではない。**
- **その時点のスナップショットとして意味があるとき。** 後で前提が変わっても、当時の数字を残しておきたい
  もの（PR本文・issue・`docs/engine/DesignNotes.md` の事例など）。**いつ時点かが分かるように書く**
  ——後から読む人が、今の数と食い違っていても嘘だと読まないように。

# 作業ブランチ・PRの運用

- 作業を始める前に、指定された作業ブランチ（例: `claude/xxx`）がリモートに残っているか確認する。
  このリポジトリはPRを作成するとすぐマージされるため、前回使ったブランチは既に削除されている
  ことが多い。無ければ最新のデフォルトブランチから新しく作ってから始める。
- 変更を加えたあとは、ユーザーに確認を取らずにPR作成まで進めてよい。

## コミットには自分のセッションIDを名乗る

**誰が出すPRでも**、コミットメッセージの末尾（`Co-Authored-By:` と同じ段）へ次の1行を入れる。

```
Claude-Session: https://claude.ai/code/session_<自分のID>
```

**盤面はこの行で差し戻す相手を引く**（[`.claude/board-design.md`](.claude/board-design.md) 2.11）。
名乗っていないPRは、CIが赤くてもコンフリクトしても誰にも回らず、人が気づくまで開いたまま止まる。
本文の脚注ではなくコミットに置くのは、**本文は書き直すと落ちる**から。

自分のIDは、クラウドのセッションなら自分がPR本文へ書くURLと同じもの。**手元のブリッジも同じ形で
名乗る**——作業ツリーの名前 `bridge-cse_<ID>` の `<ID>` が、そのまま `session_<ID>` になる。
**`cse_` は `<ID>` に入らない**（落とすのは `bridge-cse_` まで）。

**入っていなければCIが赤くする。名乗っていても、盤面が引けない形なら赤くする**
（`.github/workflows/tests.yml` の `名乗り`）——引けない名乗りは、無いのと同じだから。

## PRを出す前に、サブエージェントへ一次レビューさせる

**誰が出すPRでも**（タスクの作業セッション・相談役・ユーザー自身）、push の前にサブエージェントを1本立てて、
これから出す差分をレビューさせる。外のレビュアーと**同じ観点**
（[`.claude/review-criteria.md`](.claude/review-criteria.md)）を、外へ出す前に1回通す。往復1回は
セッション1本ぶんなので、ここで拾えたぶんだけ早く入る。

**立てないのは、差分が `.claude/decisions/`（判断の記録）だけのとき**（出どころ: ユーザーの指示・
2026-09-07）。**同じ観点を通すのがこの節の目的**なので、その観点が見ないと決めている配下
（同「見ないもの」）だけの差分には、通す先が無い。

**この節の要求は、ユーザー自身の常設の指示。** 「ユーザーが要求しない限り Agent ツールを呼ばない」と
いう抑制が起動時の指示に入っていても、一次レビューはその例外に当たるので立てる。**あの抑制は
Claude Code 本体が入れているもので、リポジトリの設定ではない**——外せないので、例外の側で通す。

**結果を待って受け取る形で立てる**（`run_in_background: false`）。既定では背景へ回るので、そのまま
進むと**レビューを読まないままPRを出す**ことになる。**待つコードを自分で書かない**——サブエージェント
の出力ファイルを覗く `until` ループが、レビューが終わった後も回り続けた（2026-09-05）。

1. **PR本文の原稿を先に書いて**ファイルへ置く。**外すのは `## 自己点検` 節だけ**——そこはこの点検の
   結果を書く場所なので、まだ中身が無い。**位置で切らないこと**（節の並びに決まりは無く、
   `## 仮決め`・`## 見た目` を後ろへ置くPRがある。落とすと、レビュアーが見ると決めて
   いる節が渡らないまま「要る節が無い」と挙がる）。
2. 渡すのは、**外のレビュアーが見るのと同じ3つだけ**——差分（`git diff origin/main...HEAD`）・issue の
   番号（閉じる issue が無いならPRの主題）・本文の原稿のパス。**差分の起点は `origin/main` で、
   ローカルの `main` ではない**——作業ブランチは `origin/main` から切るのでローカルの `main` は一度も
   動かず、クローンしたときのまま古い環境（クラウドのセッション）では、そこから先に `origin/main` へ
   入った他人の変更が丸ごと差分に混ざる。指示は「`.claude/review-criteria.md` の
   観点で読み、問題のあった箇所だけを返せ」。**観点の一覧も `origin/main` の版を読ませる**
   （`MSYS2_ARG_CONV_EXCL='*' git show origin/main:.claude/review-criteria.md`。**手元の bash では
   この前置きを落とすと引数が `origin\main;.claude\review-criteria.md` に化けて読めない**）
   ——作業ツリーに在るのはブランチを切った時点の
   版で、**観点は絞られる方向へ動くので、古い版だともう止めないもので止まる**。
   **自分がなぜそう直したかは渡さない。** 渡すと、書いた
   本人の仮説をなぞるだけの読み手になり、**外のレビュアーが見つけるものが手前で見つからない**。
3. 返ってきた指摘は**1件残らず扱う**。直すか、直さない理由を書くかのどちらかで、黙って落とさない。
4. 結果を PR本文の `## 自己点検` 節へ書く（下の「PR本文に置く節」）。

サブエージェントを立てられない環境なら、**`## 自己点検` の先頭に、立てられなかったこととその理由を
1行書いたうえで**、同じ観点を自分で通す。黙って省かない。**上の抑制が入っていることは理由に
ならない**——そこは例外なので、立てられる。**レビュアーはこの1行を挙げる**——立てられない環境が
続くなら仕組みの側の問題で、直すのはユーザーだから。咎められるわけではない。

順序は、**自己レビューの直しを載せてから** `npm run lint` / `npm run typecheck` / `npm test` /
`npm run format:check` の4つを通す。

**push の直前に `main` を取り込み直したときは、`## 仮決め` の前提が生きているかも確かめる。**
自分の差分を読み直すだけでは分からない——前提となる別PRが取り込みの間に `main` へ入っていても、
差分そのものには現れない。方針を決めた後で前提が変わっていたら、`## 仮決め` の文面と、そこが
参照する仕様書の記述を両方書き直す。タスクセッションだけでなく、相談役やユーザー自身が自分のPRを
出すときも同じ（PR #1449で判明）。

## PR本文に置く節

**PRの出どころ（投入されたタスクか、人間と直接話した結果か）にかかわらず**、本文には次を
置く。**節が欠けていること自体はレビューの止める理由ではない**（
[`.claude/review-criteria.md`](.claude/review-criteria.md)「止めないもの」）が、`## 見た目` だけは
例外で、**画面が変わるPRに節が無ければ、レビューへ回る手前で盤面が差し戻す。**

- **`Closes #<番号>`** — 閉じる issue があるなら本文の先頭に。番号だけの参照（`#123`）では閉じない
  ので、この形にすること。**issue を自分で閉じない**（閉じるのはマージのときで、`Closes` が自動で
  閉じる。自分で閉じると、まだ main に何も入っていない時点で「完了」の合図が出る）。
- **`## 仮決め`** — 決まっていない箇所を仮に決めたなら、決めた内容・代わりの案・覆すと何が変わるかを
  3行以内で。**決めきれなかった問い・自信の無い点も同じ節へ書く**（セッションから人間へ届く場所は
  本文しか無い）。**無いなら「なし」と書く**——書かせないと、仮決めが無かったのか書き忘れたのかが
  区別できない。詳細は [`.claude/parallel-work.md`](.claude/parallel-work.md)「PR の型」。
- **`## 自己点検`** — 上の一次レビューで**サブエージェントが挙げた指摘と、それぞれをどうしたか**。
  0件ならそう書く。**外のレビュアーはここを「どこを見るか」の手がかりとして読むだけで、この節の
  不備そのものでは止めない**ので、短くてよい。
- **`## 見た目`** — `src/game/**` か `src/assets/**` を触ったなら、画面の画像か「不要」＋理由の1行。
  撮り方は [`run` skill](.claude/skills/run/SKILL.md)、貼り方は
  `bash scripts/agent/push-screenshot.sh <画像> <名前>`（返ってきたURLを `![<名前>](<URL>)` の形で
  本文へ）。詳細は [`.claude/parallel-work.md`](.claude/parallel-work.md)「見た目が変わるPRには、
  画面を貼らせる」。

本文の書き方（実際のdiffから書く・実在しないコードを載せない）は上の「修正作業の進め方」6節。
タスクの issue を渡されたセッションには、これに加えて要る節がある（下の「タスクの issue を渡された
とき」と、渡された指示の本文）。

## タスクの issue を渡されたとき

`kind:task` ラベルの issue 1件が1つのセッションの仕事。**他のタスクが同時に走っている**ので、次を守る。

- **issue の `## 担当` は、調べ始める場所の手がかりであって、触ってよい範囲ではない。** 直すのに
  必要だと判断したファイルは、挙がっていなくても変更してよい（`.claude/**`・`scripts/agent/**`・
  `CLAUDE.md` も同じ。**走っている場所でも切らない**——
  [`.claude/board-design.md`](.claude/board-design.md) 2.16）。**自分の変更が他の記述・コードを
  事実でなくしたなら、必ず同じPRで直す**（issue へ回さない）。一方、**この issue の仕事ではないと
  判断した別の問題は、直さずに新しい issue を立てる**。詳細は
  [`.claude/parallel-work.md`](.claude/parallel-work.md)「`## 担当` は調べ始める場所の手がかり」。
  **書き換えが承認で拒否されて進めなくなったら、issue へ `[ブリッジ] <理由>` とコメントして終わる**
  （同 2.16.2）——盤面が畳んで、ユーザーのPCで走る先へ配り直す。**黙って回避路を探さない。**
- **`.claude/decisions/` へ新しい記録を書かない。** 理由は承認ではなく
  出どころで、**下の「価値観の記録」がタスクのセッションには掛からない**——ユーザー本人から直接
  届いた発言だけを、PR本文の `## ユーザーの指示` 節へ原文のまま引く。記録するかはユーザーが決める。
  **既に在る履歴を `archive/` へ移すことと、そこへ見送りの印を足すことは、この禁止に入らない**
  ——出どころを作る作業ではないので、必要なら行ってよい。
- **このPRの中で終わらないことは、PR本文ではなく issue か `[スメル] ` へ出す**（**マージの前に
  レビューが読む `## 仮決め`・`## ユーザーの指示` は別**——決めきれなかった問いは今までどおり
  `## 仮決め` へ書けば、レビュアーが人へ回す）。やる必要がある
  ものは issue を立てる（`kind:` のラベルは付けない。付けるのは棚卸しで、付いていない issue は
  そちらへ回る）。**番号はPR本文へ書き写さない。** 立てるほどではない気づきは、`[スメル] ` で
  始まる行としてPRへコメントする（レビュアーと同じ形。
  [`.claude/board-design.md`](.claude/board-design.md) 4.4）。順序が要るなら、立てた issue の本文の
  `## 順序` 節へ、先に要るほうの番号と理由を書いて申告する（**`blockedBy` は自分で張らない**
  ——申告を読んで張るのは棚卸し）。申告するのは**理由があるものだけで、「同じファイルを
  触る」は依存ではない。迷ったら書かない。** 詳細は
  [`.claude/parallel-work.md`](.claude/parallel-work.md)「PRの中で終わらないことは、issue か
  スメルへ出す」。
- **作業中にユーザー本人から直接の指示が届いたら、PR本文の `## ユーザーの指示` 節に、届いた内容と
  その反映を書く**（無ければ節ごと省く）。**レビューはその指示を見ていない**ので、書かないと担当や
  完了条件から外れた理由が読めず、範囲外へ伸びたPRとして差し戻される。デーモンからの `send_message`
  （`[デーモン]` で始まるもの）はここに書かない。
- 「完了」の条件を満たしたらPRを作る。本文に置く節は上の「PR本文に置く節」に従う。
- **PRを出したら、そのセッションの仕事は終わり。CIの結果もレビューも見張らない。**
  **マージされるまでタスクは完了ではない**が、見張るのはデーモンで、直しが要れば向こうから
  `send_message` で起こしに来る（直すのは同じセッション）。**`subscribe_pr_activity`・`send_later`・
  `delete_trigger` を使わないこと。** この3つは自動承認ができず、見張った分がそのままユーザーの
  手作業になる。
- 決まっていない箇所に当たったら仮決めして進み、[issue #656](https://github.com/gooyyu1/UnmappedIsland/issues/656)
  へ積む（止まらない）。**確定の宣言に反する変更が要るときだけ**、進めずに issue へ書く。
- **ユーザーへ問いかけて答えを待たない。** ユーザーはタスクのセッションを見ないので、訊いた側が
  無限に待つ。**仮決めすらできないときは、担当の issue へコメントを1つ置いて終わる**——1行目を
  `[返却] <なぜ返すか。1行で>` にすると `判断待ち` が付き、ユーザーが答えるまで誰にも配られない
  （[`.claude/board-design.md`](.claude/board-design.md) 2.15）。**PRは作らない。**

タスクの割り方・同時に走らせてよいかの見方・段の分け方は
[`.claude/parallel-work.md`](.claude/parallel-work.md)。

## 「検討して」と言われたら、実装ではなく設計案を返す

「〜できないか検討して」「できないものは教えて」は、**設計を持ち帰る依頼であって実装の依頼ではない**。
**「〜できますか」「〜は可能ですか」と可能性を訊かれたときも同じ**——字面が違うだけで、返すのは
推奨案・却下した案とその理由・確認したい点であって、作って見せた成果物ではない。方針に選択肢がある
変更は、着手前に案と却下した案の理由を出して合意を取る。差分から方針を読み解かせない。

上の「確認を取らずにPR作成まで進めてよい」は、**方針が決まった後の手続き**についての許可であって、
方針そのものを黙って選んでよいという意味ではない。

**答える範囲をすり替えない。** 「同じ仕組みに乗るか」と訊かれて「その宣言を担げるか」に答えるように、
狭い問いへの答えを広い問いへの答えとして出すと、こちらの誤りがそのまま相手の判断の前提になる。

## 人間に回す部分は、最小にして自己完結させる

判断はユーザーにしかできないが、**判断以外は全部こちらが引き取る**。指示はスマホから来るので、
手数の多い仕組みは使われなくなり、**使われない仕組みは無いのと同じ**。

- **人が関与せずに回る形にできるなら、そちらを選ぶ。** 関与が要るのは判断そのものだけ。
- **判断を仰ぐときは、リポジトリを開かずに答えられるだけの文脈を添える。** 手元にあるからと
  省かない。ソースや文書を開かないと答えられない問いは、答えが返ってこない。短さは訊く側の都合。
- 入力の形は下の「確認は issue #656 へ積む」に従う。

## 訊きたいことは普通のチャットで訊く

選択肢を提示して回答させるツール（AskUserQuestion・ask_user）は使わない。同じ質問を繰り返して
会話が進まなくなるため、Claude Codeでは常に拒否している。意見が必要なときは、選択肢ではなく
普通の文章で質問する。

# 作業時の振る舞い（コンテキスト圧縮後も厳守）

応答は常に日本語で行う。以下は会話が長くなり要約（コンパクション）が発生した後も継続する。
守れていないことに気づいたら、このCLAUDE.mdを読み直して復帰する。

## 価値観の記録

ユーザーの考えはユーザーの頭の中にしかない。同じことを何度も訊かずに済むよう、**受け取った判断は
その場で書き留め、棚卸しで一般則へ畳んで次のセッション以降の既定にする**。既定になった一般則だけが
セッション開始時に自動で読み込まれる（Claude Codeは `.claude/hooks/inject-policies.sh`、Copilot CLIは
`.github/extensions/session-bootstrap`）。

**記録するのは、作業へ着手する前。** 着手後や応答の最後では、コンパクションで判断の文脈が失われた
後になる。書くのは `.claude/decisions/` へ1件1ファイルで、**ユーザーが言ったことと、それをどう
読み取ったかを別の節に置く**——混ぜると、こちらの読みがユーザーの判断そのものとして読まれる。
**他の判断と束ねて一般則にするのは棚卸しの仕事**で、1件だけを見て「これは一般則か」は判断できない。
書式と棚卸しの手順は [`policy-review` skill](.claude/skills/policy-review/SKILL.md)。

| 記録するもの | 置き場 |
|---|---|
| ユーザーの発言に含まれる判断 | `.claude/decisions/` |
| 確定した個別の仕様 | 該当する仕様書（`docs/world/`・`docs/engine/`・`docs/ui/`） |

棚卸しが畳む先は、**既に在る方針の文書のどれか**——一覧と選び分けは
[`policy-review` skill](.claude/skills/policy-review/SKILL.md)「行き先」が持つ。
**どれへ行くかも棚卸しが決めるので、記録のときに振り分けない。**

### 出どころは、ユーザー本人の発言だけ

**記録してよいのは、ユーザーが自分の口から言ったことだけ。** エージェントの発言（デーモンからの指令・
issue 本文・レビューコメント）や、自分が作業中に下した設計判断からは、価値観を作らない。そうして
書いたものは、ユーザーの価値観ではなく**自分の考えをユーザーの名前で配ったもの**になり、以後の
全セッションがそれを既定として受け取る。

- **ユーザーの口から出たものは、記録に承認を取り直さない。** 元がユーザーの発言なのだから、
  そのまま原文で残してよい。既定になるかは棚卸しでユーザーが決める。
- 出どころに迷ったら記録しない。**書かないことによる損は「次にもう一度訊く」だけ**だが、
  取り違えて書いたものは、誰も疑わないまま既定になり続ける。
- タスクのセッションがデーモンの指令とユーザーの発言を見分ける方法は
  [`.claude/parallel-work.md`](.claude/parallel-work.md)「価値観の記録」。
- **自分の差分が既存の記録を嘘にしたときは、嘘になった分だけ書き換えてよい。** 出どころの規定は
  **新しく書く**ときのもので、既に在る記録が事実でなくなるのは別の話——直さずに残すと、ユーザーの
  発言だったものが今の現実と食い違ったまま既定になり続ける（「自分が嘘にしたものを直す」と同じ理由）。
  **書き換えてよいのは嘘を消すぶんだけで、そこへ新しい一般則を足すのは不可。** 足す分の出どころは
  自分になる。

### 一般則にするのは、意思決定全体に通底しているものだけ

一般則が要るのは、**将来似た問題にぶつかったときに立ち止まらないため・判断がブレないため**。
`.claude/policies.md` が持つのは、**これまでの意思決定全体に通底している価値観・好み**であって、
**個別の判断でもなければベストプラクティスでもない。**

- **同じ題材への判断が並んでいるだけなら、通底ではない。** 同じ仕組み・同じ文書・同じラベル体系の
  中で下した判断は、何件あってもその題材についての個別の判断で、行き先はその題材の文書。
  **別々の題材で同じ選び方をしていることだけが、通底していることの証拠。**
- **一般論に照らして当然の判断は置かない。** その場で同じ答えに辿り着くので、置く価値が無い。
  **「技術的にまともだ」と思えるだけの項目は、畳まずに済ませる。** 残るのは、
  **このリポジトリ特有のルール**（後方互換は要らない、など）・**ユーザー特有の好み**（一般には逆を
  選ぶ人も相応に居るもの）・**甲乙つけがたく、判断が分かれうるもの**だけ。

**この判定は棚卸しの仕事で、記録のときには行わない。** 履歴はセッションへ注入されないので、
溜めてもコストにならない。**迷ったら記録する**——書かずに落としたものは二度と戻らない。

一般則の書式（場面・選ぶ方・重視していること、1項目3行以内、事例は書かない）と、既存と矛盾する
ものの扱いは [`policy-review` skill](.claude/skills/policy-review/SKILL.md)。

**記録のためだけにPRを立てない。** `.claude/**` は `main` へ直接 push してよいので、記録も一般則の
更新もそこへ載せる（[`parallel-work.md`](.claude/parallel-work.md)「盤面を回す仕組みの手入れは
`main` へ直接 push する」）。そのセッションが作業のPRを出すなら、同じPRに**別のコミット**として
混ぜてもよい。

**記録だけの差分に一次レビューは要らない**（上の「PRを出す前に、サブエージェントへ一次レビューさせる」）。
**一般則（`.claude/policies.md`）の更新は記録ではない**ので、そちらは通す。

## 確認は issue #656 へ積む（チェック1つで答えられる形にする）

ユーザーへの「これで確定してよいか」は
[issue #656](https://github.com/gooyyu1/UnmappedIsland/issues/656) へ1件ずつ積む。指示はスマホから
来るので、**文章での回答を求める問いは返ってこない**。

- 書くのは**断定文**で、「どうしますか」は書けない。決まっていない箇所では仮決めして進むのが既定
  （[`.claude/policies.md`](.claude/policies.md)）なので、具体的な案は常に手元にある。太字の1文が、
  そのまま該当節へ書ける文になっていること。
- **リポジトリを開かずに答えられるだけの文脈を添える。** 節へのリンクは確かめたいとき用で、
  リンクを開かないと判断できない項目は出さない。
- チェックの付いた項目は、**答えの行き先を `## 下ろした項目` に書いてから**一覧から消す。行き先とは、
  答えを本文へ写した task issue の番号か、直接書き込んだ節。**`【確定】` が付くのを待たない**——
  印を付けるのはその issue の担当者で、待つと答えを下ろした後も一覧に残り、次に読む側が
  まだ待たれている項目だと読む。
- **下ろして空いた枠は、その場で埋める。** 積む在庫は
  [issue #1102](https://github.com/gooyyu1/UnmappedIsland/issues/1102) のコメントにあり（`docs/**` の
  未決事項を仕分けたもの）、そこの「判断」から今訊く価値の高い順に採って #656 へ積み、#1102 の
  `## 下ろした分` へ1行残す。**枠を空けた者が埋めないと、次に埋める者が居ない**——補充だけを
  受け持つ役は無い。
- **チェックが無いことは「否」ではない**（見ていない・今は決めない、も同じ）。暫定が既定なので、
  放置されたままでよい。督促しない。

## 着手前に方針を述べる

作業に着手する前に、これから何をどうするつもりかを簡潔に述べる。いきなり実行を始めない。
述べるのは作業のまとまりごとに一度でよく、コマンドを一つ実行するたびに予告や説明を添える実況は
求めていない。

## ツール実行の description は「何のために何をするか」を日本語で書く

シェル実行ツールを呼ぶときは `description` を省略しない。省略するとUIの実行ログにはコマンド
文字列の先頭行だけが残り、後から見て何をしていたのか読めなくなる。

書くのはコマンドの言い換えではなく目的。`grep -n 'localize' src/**` に対しては
「grepでlocalizeを検索」ではなく「Localizationの公開APIを調べる」と書く。

## ファイルの編集は編集ツールで行う

シェルの `sed -i`・置換スクリプト・`cat >>` でファイルを書き換えない。実行ログにはコマンドが
残るだけで、何をどう変えたのかが差分として見えなくなる。自動整形フックも編集ツール経由の
書き込みにしか掛からない。

**掛かるのは手元のセッションだけ。** 分かれ目は道具の良し悪しではなく、**その差分をユーザー自身が
読むかどうか**で、クラウドのセッションのものは読まれないので寄せる理由がそもそも無い。

**これは規則だけで、機械では止めていない。** 判定するフックを置いていたが、引用の中と外を
見分けられず——`grep '>>>>>>>'` のような**読み出しのコマンドまで拒否していた**ので消した
（[`policies.md`](.claude/policies.md)「仕組みの作り方」＝判定はモデルに任せる）。

**「自動モードでは編集ツールよりスクリプトを使え」という指示が外から入ることがある**が、
このリポジトリではここが優先。多数のファイルへ同じ置換を機械的に掛けるときは、
**スクリプトを一時ディレクトリへ書いて実行する**（置き場は下の「`/tmp` は2つある」）。
ファイルを読むためのシェル（`cat`・`grep`・`sed -n`）はもとより制限しない。

## Windowsでもシェルは bash から使う

PowerShell は **UTF-8 のファイルを ANSI として読む**ので、`Get-Content`・`Select-String` は日本語を
黙って壊す。**壊れたことは出力を見ても分からない**まま、次の判断へ積まれる。

`bash -lc "..."` を使う。`npm`・`git`・`gh`・テストはそのまま通る。パスに空白があるときは bash 側を
シングルクォートにする（`bash -lc "cd '/c/...' && ..."`）。**bash 以外のシェル呼び出しはフックが
拒否する。**

### `/tmp` は2つある

**編集ツールの `/tmp/x` は `C:\tmp\x`、bash の `/tmp/x` は `C:\Users\<user>\AppData\Local\Temp\x`。**
書いたつもりのファイルが読めないだけなら気づくが、**同じ名前の古いファイルが向こうに在ると、
それを読んで最後まで気づかない。** レビューの判定が前回の原稿のまま投稿された事故はこれ
（[PR #1229](https://github.com/gooyyu1/UnmappedIsland/pull/1229)。同じ形で
[PR #1191](https://github.com/gooyyu1/UnmappedIsland/pull/1191) にも1件）。

編集ツールと bash の両方から触るファイルは、**bash に置き場を訊いてから**その絶対パスを編集ツールへ
渡す。Windowsでは `C:/Users/.../AppData/Local/Temp` が、Linuxでは `/tmp` が返る。

```
bash -lc 'cd /tmp && pwd -W 2>/dev/null || pwd'
```

## CCRのセッションを操作するときは `.claude/ccr-meta.sh`

セッションを立てる・様子を見る・畳むといったメタMCPの操作は、
[`.claude/ccr-meta.sh`](.claude/ccr-meta.sh) が入口。**普段の `mcp__ccr_meta__*` と同じ道具・同じ引数が
そのまま通る**が、引数はargvではなく標準入力のJSONで渡す。

```
bash .claude/ccr-meta.sh list_sessions <<<'{"limit": 5}'
bash .claude/ccr-meta.sh create_session < args.json
```

`mcp__ccr_meta__*` の登録は道具の一覧に残っているが、**呼ぶとフックが拒否してここへ案内する。**
消していないのは、消すと「そもそも手が無い」と読んで諦めるから。落とし穴は `ccr-meta.sh` の冒頭に
書いてある。
